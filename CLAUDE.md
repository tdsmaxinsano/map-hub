# DependableCare Portal — Project Index

This is the master reference for the DependableCare internal staffing portal. Always read this file first, then read the specific CLAUDE file for the area you are working on.

---

## Project Structure

All files are standalone HTML/CSS/JS — no framework, no build step. They share the same Supabase project and Mapbox token.

| File | Purpose | Reference Doc |
|---|---|---|
| `clinician-map.html` | Main map dashboard — clinician locations, filters, referral overlay, AI assistant (~24,000+ lines) | `CLAUDE-map.md` |
| `referrals.html` | Referral board — table view, contacts, audit, new referral form | `CLAUDE-referrals.md` |
| `time-tracker.html` | Time tracking — clock in/out, pay periods, approvals, HUD | `CLAUDE-timetracker.md` |
| `compliance.html` | **Compliance Hub** with selectable trackers. Landing screen has two tiles: 📊 **Frequency Compliance** (visit-frequency tracking — active queue + resolved log, TB Create-bills HTML import) and ✅ **Evaluation Compliance** (TB Active Services + Patient List driven — categorized queue grouped by Issues/Hold/Re-Eval/No Sched/Scheduled-Late/To Sync/Completed/Assigned, color-coded by sync status: 🟢 synced / 🟡 eval done not synced / ⚪ no evaluation / ⬜ cancelled-term-returned). URL-driven view state (`?view=hub|frequency|evaluation`). Evaluation rows carry **non-editable `patient_id` + `referral_id` anchors** from TB so future cross-report joins (Patient Visits, Documented Procedures, AR Aging) are trivial. Inline per-row editing of category / sync status / notes (debounced, surgical updates). **Two-file import**: TB Active Services CSV + Patient List XLSX (SheetJS), joined client-side on Last+First+DOB+Referral date, upserts on (patient_id, referral_id). Auto-promotes rows to Hold/Hospital when TB has a hold flag set. Status Notes cell carries 4 stacked lines: editable user note + 3 read-only collapsible TB references (Episode instructions, Service protocol, Hold details). Preserves user-edited columns across re-imports. | (this file + comments in compliance.html) |
| `kanban.html` | Portal-wide kanban task board — multiple boards, SLAs, drag/drop, admin board management, staff profiles | `CLAUDE-kanban.md` |
| `roster-review.html` | **Bulk clinician editor for HR / coordinator review sessions.** Sticky thead + sticky Name column for big-table scrolling. Inline pause editor under each clinician's name when status=Paused (dropdown: Personal / Sick / Vacation / Other-with-text). Status pill + discipline pill stacked under each name for at-a-glance context. Performance metrics (Avg Sync Days / Avg Visits/Run / Avg Visit Time from VDR aggregates) shown directly after Status. **EMR chips are click-to-toggle** (Strata / TherapyBoss / Axxess / Kinnser — writes to same `emr_capabilities` JSONB the map reads). Restrictions opens a drawer with roster-only typeahead (agencies + clinicians; no free-text). Notes drawer. **Phone & Email read-only** with 🔒 indicator (TB is source of truth). **Emergency Contact** column (name / phone / relationship) with drawer editor — managed here since TB doesn't track it. ✓ Reviewed checkbox stamps `last_reviewed_at`/`last_reviewed_by`. Filters: status / discipline / "Need Review" / "Returning soon" / "Paused 30d+". Multi-select bulk actions + CSV export. Surgical EMR toggle keeps scroll position locked; other re-renders save/restore scroll + focus. Admin + Editor write access. | (this file + comments in roster-review.html) |
| `agencies.html` | **Admin-only 🏢 Agencies tool** — single source of truth for HH agency contract rates + payment behavior + collections contacts. Sticky thead + sticky Name column. Inline editors throughout: 6 rate cells (OT Eval / OTA / PT Eval / PTA / ST Eval / ST Other) as `<input type=number>` with debounced save; 2 rating cells (💵 Payment / 💬 Communication) as click-to-cycle chips (null → 1 → 2 → 3 → 4 → 5 → null) with green/amber/red tint; Active toggle pill; contract location / preferred payment method / sent via / collections contact as text inputs; notes drawer (click cell → side drawer). Stats strip shows total / has-contract / unrated-vs-rated histograms for both ratings. **📥 Import XLSX** modal accepts the user's existing `HH RATINGS.xlsx` shape unchanged (sheet `Agencys`, columns `AGENCY NAME / OTF / OTFA / PTF / PTFA / STE / STEO / Payment / Communication / CONT_Location / ACTIVE / Contract Start Date / Preferred Payment Method / Sent Via / Contact Collections / Notes / Comments`); SheetJS-parsed, client-side normalized-name preview shows matched/unmatched count; unmatched rows get a typeahead-style dropdown to manually pick from existing portal agencies or skip. Server-side `upsert_agency_contracts` RPC does the authoritative match via `normalize_agency_name()` helper (lowercase, strip punctuation + Inc/LLC/Ltd/Corp/Services suffixes, collapse whitespace). Two-layer admin gate (shell tab hidden for non-admin + page-level role check shows a "🔒 Admin only" gate card). Agency name link in shell uses `parent.shellSwitchTab('map', 'agency=...')` to open the map's existing agency profile. **Map untouched** — no rate/payment chips on map profile in this rev (intentional; future hook). | (this file + comments in agencies.html) |
| `finance.html` | **Admin-only Finance area** with sub-views: Payroll (office-staff summary linking to Time Tracker + 🗓️ **Clinician Payroll Calendar** — yearly bi-weekly Sun→Sat periods, pay date = period_end + 16 days = Monday, anchor 2025-12-14, with print + CSV download + year nav) and **Billing**. Billing has 4 cards: ① **VDR Runner** ingests a `Create bills.xlsx` from TherapyBoss in the browser (SheetJS), flags discrepancies (OutOfRange / ShortAndBillable / MissedVisit / EvalTimeAlert), shows inline stats + Chart.js charts + tabbed discrepancy tables, generates a downloadable multi-sheet XLSX (ExcelJS); ② **Preview** (auto-shown after a run); ③ **History & Trends** persists every saved run to `vdr_runs` + `vdr_clinician_metrics` + `vdr_payer_metrics` with the report XLSX in `vdr-reports` bucket, history table with payroll-date markers (bi-weekly Wed before 2024-10-17, Thu after), **📊 View** button rebuilds the inline preview from the stored `prepared_rows` JSONB; ④ **Invoice Converter (IIF)** drag-drops a raw QuickBooks IIF, strips 11 cruft columns + filters TAXABLE/negative rows + renames headers + substitutes `Medicare → Medicare Part B.`, downloads cleaned `.iif` ready for SaaSant → QB upload (port of the user's local Python script). Two-layer admin gate (shell tab + page-level). | (this file + comments in finance.html) |
| `chat-widget.js` | Shared team-chat widget — included on every portal page; old-school single-channel team chat with presence + sound + tab-flash | `CLAUDE-kanban.md` (chat section) |
| `index.html` | **Portal shell** — owns the login/signup/forgot-password screen, header tabs, and 5 lazy-loaded iframes (one per tool). Home pane is a stats dashboard. State preserves across tabs. | (this file + comments in index.html) |

**Iframe shell (May 2026):** `index.html` is now an iframe shell instead of a simple redirect. The header hosts 6 tabs (🏠 Home · 🗺️ Map · 📋 Referrals · ⏱️ Time · ✅ Compliance · 🗂️ Kanban). Each tool tab is an `<iframe>` whose `src` is set lazily on first click. Subsequent clicks just toggle `hidden`, so the iframe's state (map zoom, kanban board, scroll, filters) is preserved. URL is `?tab=<id>` and updates via `history.pushState`; back/forward works natively. Exposes `window.shellSwitchTab(tabId, queryString)` so tools (in iframes) can call up to the shell to cross-navigate without losing sibling state.

**Per-tool iframe-detection:** Each tool HTML has a tiny script in `<head>` that adds `html.in-iframe-shell` when `window.self !== window.top`. CSS rules then hide each tool's duplicate chrome (portal nav strip, brand, user chip, sign-out) when running inside the shell. Tools accessed via direct URL still show their full header — backward compatible.

**Portal nav strip** (🏠 Home / 🗺️ Map / 📋 Referrals / ⏱️ Time / ✅ Compliance / 🗂️ Kanban / 📋 Roster / 🏢 Agencies / 💰 Finance) is the fallback navigation when a tool is loaded standalone. 🏠 Home is always first and links back to `index.html` (the shell's Home dashboard pane). 🏢 Agencies and 💰 Finance are admin-only-revealed. Uses real `<a href>` links so Ctrl/Cmd+click → new tab works for free. All pages use `localStorage` auth → SSO across the portal, no re-login when switching tabs.

---

## Shared Credentials

### Supabase
- **URL:** `https://jpemlcuxjvynlbeygukb.supabase.co`
- **Project ref:** `jpemlcuxjvynlbeygukb`
- **Anon key:** hardcoded in each file (search `supabaseKey`)
- **Auth:** `localStorage`-based in **all** files (clinician-map.html switched from `sessionStorage` → `localStorage` in May 2026 so sessions survive tab closes / browser restarts; required for the upcoming RLS rollout to not break first-time logins on a fresh browser)
- **RLS:** Phase 1 ON (May 2026, applied via `01_phase1_enable_rls.sql`); 23 core tables locked to `authenticated`-only. Rollback available at `01_phase1_rollback.sql` if needed.
- **Self-service signup + password reset:** the shell login screen handles 4 modes — Sign In · Sign Up · Forgot Password · Set New Password. Forgot triggers `db.auth.resetPasswordForEmail` with `redirectTo = origin + pathname` so the recovery link returns to the shell. Recovery is detected via the URL hash captured **before** Supabase init (race-condition-proof), and `inPasswordRecovery` suppresses Supabase's auto-`SIGNED_IN` event so users actually get to choose a new password before being logged in.

### Supabase Edge Functions
| Function | Purpose | Secrets used |
|---|---|---|
| `send-reactivation-email` | Emails hiring manager when staff requests reactivation of an Inactive clinician | `SENDGRID_API_KEY`, `SENDGRID_FROM_EMAIL`, `HIRING_MANAGER_EMAIL` |
| `send-import-report` | After successful Sync Update or ZIP coverage import, emails change report to logged-in user (CC list optional) | `SENDGRID_API_KEY`, `SENDGRID_FROM_EMAIL`, `IMPORT_REPORT_CC_EMAIL` (optional, comma-separated) |
| `tb-scan-image` | Proxies TherapyBoss screenshot scan to OpenAI gpt-4o vision; key never leaves Supabase | `OPENAI_API_KEY` |
| `tasks-auto-archive` | Daily cron — flips `is_archived = true` on kanban tasks completed 7+ days ago | (uses default `SUPABASE_SERVICE_ROLE_KEY`) |
| `auto-transition-pauses` | Daily cron — finds clinicians with `status='Paused' AND pause_end_date < CURRENT_DATE` and flips them back to Active in both `clinician_profiles` and `clinician_v2`, writing an audit row to `clinician_profile_status_log` with reason "Auto-transition: pause end date passed". Schedule: 11:00 UTC = 6 AM Chicago. Powers the pause lifecycle feature (Home alerts board). | `SUPABASE_SERVICE_ROLE_KEY` (default) |

All functions have CORS headers and use JWT verification (signed-in users only). Deploy via Supabase Dashboard → Edge Functions or CLI (`supabase functions deploy <name> --project-ref jpemlcuxjvynlbeygukb`).

**Note on `tb-scan-image` API key**: The OpenAI key for the AI scanner ("Scan with AI" in TB Import modal) lives in Supabase Edge Function secrets (`OPENAI_API_KEY`). If staff see "Missing scopes: model.request" — that's OpenAI's restricted-key error, not Anthropic. Fix: create a new OpenAI key with **All / Default permissions** (NOT "Restricted") and update the secret.

### Supabase Storage Buckets
| Bucket | Purpose | Access |
|---|---|---|
| `clinician-photos` | Clinician headshots (existing) | Per-clinician RLS — needs follow-up review |
| `staff-photos` | Portal user headshots used by chat + kanban + future avatars | Public-read, admin-write (`03_staff_directory.sql`) |
| `reimbursement-receipts` | Receipt images/PDFs attached to Time Tracker pay-period reimbursement adjustments | Public-read, admin-write (`21_reimbursement_receipts.sql`) |

### Supabase SQL migrations (in `supabase/policies/`)
Run from Supabase Dashboard → SQL Editor in numerical order. All idempotent — safe to re-run.

| File | Purpose |
|---|---|
| `01_phase1_enable_rls.sql` / `01_phase1_rollback.sql` | Phase 1 RLS — locks 23 core tables to `authenticated` only. **APPLIED May 2026.** |
| `02_kanban_tables.sql` | Kanban schema: `boards`, `board_columns`, `tasks` + RLS + seed data for 3 default boards |
| `03_staff_directory.sql` | Adds `display_name`, `display_color`, `photo_url` cols to `staff_config`; creates `list_staff()` and `upsert_staff_profile()` SECURITY DEFINER RPCs; creates `staff-photos` storage bucket |
| `04_chat_messages.sql` | Team chat schema + RLS + adds the table to `supabase_realtime` publication so realtime broadcasts work |
| `05_auto_user_role.sql` | Backfills `user_roles` for orphan auth users + adds `on_auth_user_created` trigger that auto-inserts a `readonly` user_roles row on every `auth.users` INSERT. SECURITY DEFINER. Idempotent. **Run this any time after a fresh DB or to catch up older orphan accounts.** |
| `06_auto_staff_config.sql` | Backfills `staff_config` for orphan auth users + adds `on_auth_user_created_staff` trigger that auto-inserts an inactive `staff_config` row (`is_active=false`, `hourly_rate=0`, `pay_type='philippines'`, `timezone='Asia/Manila'`, `display_name = SPLIT_PART(email, '@', 1)`) on every signup. Lets new signups appear in the time-tracker admin dashboard "Needs setup" section without an admin having to copy UUIDs. SECURITY DEFINER. Idempotent. |
| `07_roster_review_fields.sql` | Adds three columns to `clinician_profiles`: `pause_reason TEXT`, `last_reviewed_at TIMESTAMPTZ`, `last_reviewed_by UUID`. Plus an index on `last_reviewed_at` for the "Need Review since X" filter in `roster-review.html`. All nullable + additive. Idempotent. |
| `08_vdr_billing.sql` | Creates the VDR (Visit Discrepancy Report) billing tables: `vdr_runs` (one row per processed `Create bills` export), `vdr_clinician_metrics` (per-run per-clinician aggregates for grading), `vdr_payer_metrics` (per-run per-payer revenue). Plus the `vdr-reports` storage bucket for generated XLSX files. RLS admin-only across the board. Idempotent. Drives the Billing sub-view in `finance.html`. |
| `09_clinician_vdr_summary.sql` | Adds `list_clinician_vdr_summary()` SECURITY DEFINER RPC that aggregates `vdr_clinician_metrics` per clinician — returns `run_count`, `total_visits`, `avg_visits_per_run`, `avg_sync_days` (visit-weighted), `avg_visit_minutes` (visit-weighted, NULL-aware), `total_charges`, `latest_run_at`. Surfaces in Roster Review (three default-on columns) + clinician profile in the map (three chips in the header quick-facts row). Aggregates are SECURITY DEFINER so editors see the numbers even though per-run rows remain admin-only. Idempotent. Self-healing: also re-adds `vdr_clinician_metrics.avg_visit_minutes` in case 08 hasn't been run. |
| `10_emergency_contact.sql` | Adds three columns to `clinician_profiles`: `emergency_contact_name TEXT`, `emergency_contact_phone TEXT`, `emergency_contact_relation TEXT`. Drives the Emergency Contact drawer in `roster-review.html`. TB doesn't track emergency contacts, so this is portal-managed. Idempotent. |
| `11_pause_lifecycle.sql` | Adds `list_pause_alerts()` SECURITY DEFINER RPC that returns active pause/vacation alerts (pause_ending_soon / pause_ending_today / pause_starting_soon / pause_starting_today / just_returned). Drives the Home page 📰 News & Alerts board. Pause-end alerts: 1 day before + day-of; pause-start alerts: **14 days before through day-of** (widened from the original 3-day window for coverage planning); just-returned: visible 5 days after auto-transition. Read-only, authenticated-accessible. Idempotent. |
| `12_evaluation_compliance.sql` | Creates `evaluation_compliance_items` (one row per (patient_id, referral_id) anchored to TB's Active Services report) + 4 RPCs: `list_evaluation_compliance()` (SECURITY DEFINER read), `upsert_evaluation_items(rows JSONB, batch_id UUID)` (bulk import — dedupes inbound payload via DISTINCT ON, preserves user-edited columns on conflict), `update_evaluation_item(id, ...)` (targeted single-row patch), `clear_evaluation_compliance()` (admin-only nuke; returns row count deleted). RLS: SELECT for all authenticated, write for admin+editor, clear for admin only. Drives the Evaluation tile in `compliance.html`. Idempotent. |
| `13_eval_referral_date.sql` | Adds `referral_date DATE` column to `evaluation_compliance_items`, flips category default from `'Active'` → `'Assigned'`, migrates any existing `'Active'` rows → `'Assigned'`, and rebuilds `upsert_evaluation_items` to accept + persist `referral_date`. Drives the date-bucketed "Intake — Assigned by date" region in the Evaluation tile UI. Idempotent. |
| `14_eval_patient_list_join.sql` | Adds 5 columns to `evaluation_compliance_items` populated by the second TB report (Patient List XLSX): `episode_instructions TEXT`, `service_protocol TEXT`, `hold_since DATE`, `hold_reason TEXT`, `hold_details TEXT`. Rebuilds `upsert_evaluation_items` to accept all the new fields AND auto-promote rows with `hold_since IS NOT NULL` to `category='Hold/Hospital'` on INSERT (UPDATE preserves user-edited category). Drives the two-file import flow + the multi-line Status Notes cell (collapsible TB-managed reference lines) in `compliance.html`. Idempotent. |
| `15_eval_schedule_enrichment.sql` | Adds 9 columns to `evaluation_compliance_items` for the phase-2 Scheduled Appointments import: `next_eval_date DATE`, `next_eval_activity TEXT`, `next_eval_clinician TEXT`, `next_eval_confirmed BOOLEAN`, `next_eval_appt_id TEXT`, `next_eval_note TEXT`, `last_eval_attempted DATE`, `scheduled_appt_count INTEGER`, `schedule_imported_at TIMESTAMPTZ`. New RPC `update_eval_schedule_info(rows jsonb)` matches on (patient_id, service) and UPDATES only (no inserts — rows must exist from the base import first). Drives the "Eval Sched" column and SLA indicators (24hr-schedule / 48hr-visit / 48hr-sync rules) in the Evaluation tile UI. Idempotent. |
| `16_eval_visits_enrichment.sql` | Adds 10 columns to `evaluation_compliance_items` for the phase-3 Patient Visits import: `eval_visit_date DATE`, `eval_visit_type TEXT`, `eval_visit_clinician TEXT`, `eval_note_completed_at TIMESTAMPTZ`, `eval_completion_hours NUMERIC` (sync-delay tracker), `eval_visit_id TEXT`, `reeval_visit_date DATE`, `reeval_note_completed_at TIMESTAMPTZ`, `last_visit_actual_date DATE`, `visits_imported_at TIMESTAMPTZ`. New RPC `apply_eval_visits(rows jsonb)` UPDATES only (matches on (patient_id, service)) AND auto-derives `sync_status`: eval visit found + note completed → `'synced'`; eval visit found + note pending → `'eval_done_not_synced'`; no eval visit → sync_status untouched. Drives the merged "Eval Sched / Eval Done" cell + the 48-hr sync SLA chip. Idempotent. |
| `17_clinician_visit_period_metrics.sql` | Creates `clinician_visit_period_metrics` table (one row per clinician × bi-weekly pay period, Thursday-anchored at 2024-10-17) populated by either (a) the Eval Compliance Patient Visits import (current-quarter refresh) or (b) the Roster Review admin-only **📥 Bulk Import Visits** button (historical backfill, supports multi-year files up to ~270K visit rows). New RPCs `upsert_clinician_visit_periods(rows jsonb)` (write, with name-normalized clinician_id auto-resolution) and `list_clinician_visit_summary()` (read, same field shape as `list_clinician_vdr_summary` so Roster can swap sources transparently). Roster Review now prefers Visits-derived metrics (every visit incl. non-billable) and falls back to VDR-derived (Create-bills) when Visits hasn't been imported for that clinician. Idempotent. |
| `18_clinician_dob.sql` | Adds `dob DATE` column to `clinician_profiles` + `idx_clinician_profiles_dob_md` (month*100+day index for fast birthday-window queries). Two RPCs: (1) `list_birthday_alerts()` returns Active+Paused clinicians whose birthday is today (`birthday_today`) or tomorrow (`birthday_tomorrow`) — drives 🎂 alerts on the Home page News & Alerts board. (2) `list_birthdays_in_month(year, month)` returns every Active+Paused clinician with a birthday in that calendar month, sorted by day with `days_offset` (negative for past, 0 today, positive upcoming) — drives the Home page **🎂 Birthdays this month** drawer (prev/next month nav, grouped Today / Upcoming / Earlier-this-month, click-to-open clinician profile). DOB also surfaces in: Roster Review (default-hidden `DOB (MM/DD)` column with celebratory tint when birthday is in next 7 days), Map profile header (🎂 chip in quick-facts row). DOB is fed via the existing Map → Bulk Import flow's clinician_master mode (column aliases: DOB / Date of Birth / Birthday / Birth Date). `normalizeImportDateValue` extended to detect Excel serial dates ([1, 80000] range) since TB exports DOB without a date number format. Additions Only mode opportunistically backfills DOB for matched-existing clinicians whose DB `dob` is NULL (narrow exception so users don't need to switch to Full Initial Load). Idempotent. |
| `19_clinician_payroll_runs.sql` | Creates `clinician_payroll_runs` table (admin-only RLS) — one row per pay-period audit of the TB "Prepare payroll" Excel. Stores period bounds (Sun-Sat) + pay date (Mon = period_end + 16d), top-line totals (pay/expenses/visits/payable/non-payable/distinct clinicians), audit state machine (`open → audited → exported → paid` with timestamps + actor IDs), and JSONB snapshots (`per_clinician_payload` for the audit table, `all_visits_payload` for drill-down). Six admin-only RPCs: `upsert_clinician_payroll_run(...)` (re-import preserves audit state on conflict), `list_clinician_payroll_runs()` (summary for history table), `get_clinician_payroll_run(id)` (full detail), `mark_clinician_payroll_audited(id, notes)`, `mark_clinician_payroll_exported(id)`, `mark_clinician_payroll_paid(id)`. Drives the Finance → Payroll → 💵 Clinician Payroll Audit card (import modal, history table, per-clinician drill-down). Phase 3 (QB export) will read from these stored snapshots. Idempotent. |
| `34_clinician_payroll_medb.sql` | Adds Strata Med B as a parallel pay stream alongside TB regular Pay in the Clinician Payroll Audit. Background: each pay period every clinician's QB payable is normally split across **TWO Bills sharing the same pay-period Bill #** (e.g. `05032026-05162026`) — one for regular Pay (TB) and one for Med B (Strata EMR), both routed to the same `Subcontractor:<discipline>` GL. Schema: new `total_medb NUMERIC(12,2) NOT NULL DEFAULT 0` column on `clinician_payroll_runs`; per-clinician Med B lives **inside** the existing `per_clinician_payload` JSONB (new fields `medb_total: number` + `medb_source: string\|null`, both optional and default to 0/null on read) — additive + forward-compatible with all existing code paths. RPC `upsert_clinician_payroll_run(...)` rewritten with a new `p_total_medb` parameter (DROP-then-CREATE because adding a mid-signature param is a signature change); `list_clinician_payroll_runs()` rewritten to add `total_medb` to the return-row shape (same DROP-then-CREATE rule). `get_clinician_payroll_run(run_id)` unchanged (returns SETOF the table so the new column rides along). Existing audits load with `Med B = $0` everywhere — backward compatible, no backfill needed. Strata file remains optional on import: skipping it saves the period with Med B = 0 across all clinicians (current behavior preserved). Idempotent. |
| `35_clinician_medb_rates.sql` | Learn-as-you-go memory for each clinician's per-visit Med B rate. Backs the Strata Med B PDF parser (Phase 2): Strata has no rates configured, so the rate per visit is derived from TB's own per-visit Pay column and remembered for next period. Table `clinician_medb_rates` keyed by `clinician_lookup_key` (same normalized form as `cprClinLookupKey`: lowercase + trailing-discipline-suffix stripped) UNIQUE; carries `rate NUMERIC(8,2) CHECK >= 0`, `source` CHECK over `derived_assistant_mode` / `derived_supervisor_min` / `manual_override` / `memory_hit`, `hit_count`, `first_seen_at`, `last_seen_at`, `last_set_by`. Three admin-only SECURITY DEFINER RPCs: `lookup_clinician_medb_rate(p_lookup_key)` returns saved rate or NULL; `upsert_clinician_medb_rates(p_rows JSONB)` bulk-upserts (input shape: `[{lookup_key, display, discipline, rate, source}, …]`) with a key conflict rule that **preserves `manual_override`** rows against subsequent fresh-derivation upserts (a hand-edited rate is never silently overwritten by an auto-derivation); `list_clinician_medb_rates()` returns the full table for future admin-view UI. Drives the new `$ / visit` editable column in the CPR import preview + the rate persistence on save. Idempotent. |
| `43_repair_derived_start_dates.sql` | **One-shot data repair** for the 07/17/1905 auto start/hire dates (Excel-serial year collision — a stray `2024` read as a serial → 1905-07-17, which won the earliest-service reduce). PRs #128/#129 stopped new corruption + hid existing 1905s at read time, but the stored `clinician_profiles.tags->>'derivedStartDate'` was still 1905 (cells went blank). This recomputes each clinician's earliest **plausible** (>= 2000-01-01) completed-service date directly from `therapy_boss_completed_service_import_rows` (min across `start_of_episode`/`referral_date`/`last_visit_date`/`ended_date`) joined via `therapy_boss_completed_service_import_clinicians.matched_clinician_id`, and writes it back into `tags.derivedStartDate` — but **only** for clinicians whose stored derived value is currently corrupt (pre-2000 / malformed); explicit hire dates + good derived values are untouched. Type-agnostic (casts each column to text, regex-guards a `YYYY-MM-DD` prefix, then to date) so it can't error on a malformed source value. Idempotent (repaired rows are now >= 2000 → skipped on re-run). |
| `42_staff_expenses.sql` | **Staff expenses — self-entry (PHP/USD) + admin approve → reimbursement.** Table `staff_expenses`: user_id, expense_date, description, `amount` (as entered) + `currency` CHECK (`PHP` \| `USD`), receipt_url, `status` CHECK (`pending` \| `approved` \| `declined`), reviewed_at/by, decline_reason, `adjustment_id` (links the created `pay_period_adjustments` row). RLS: staff SELECT/INSERT own (+UPDATE/DELETE own **while pending**), admin everything. Adds a scoped write policy on the existing `reimbursement-receipts` bucket so staff can upload receipts to `staff-expenses/<own uid>/…` only. Client (`time-tracker.html`): **💸 My Expenses card** in `#staff-view` (date/desc/amount/PHP-USD/receipt ≤5MB → pending; 🟡/✅/⛔ pills with decline-reason tooltip; 🗑 withdraw while pending) + **💸 Expense Approvals card** on the admin Pay Period tab (pending list across staff; ✓ Approve inserts a `kind='reimbursement'` adjustment on the **period currently shown** with USD = amount ÷ `#php-rate` for PHP, copies receipt_url, links adjustment_id, confirm dialog shows the conversion; ✕ Decline prompts a reason). Card auto-hides when none pending or table missing. Idempotent. |
| `41_staff_pay_reviews.sql` | Office-staff **reviews & raises history** (Time Tracker → Settings → 📋 Reviews panel). Table `staff_pay_reviews`: one row per raise or review — `kind` CHECK (`raise` \| `review`), `effective_date`, `pay_field` CHECK (`hourly_rate` \| `salary_per_period` — which value the raise changed, matching migration 40's categories), `old_value`/`new_value`, `note`, `doc_path` (uploaded review doc), `created_at/by`. Index on (user_id, effective_date DESC). **Admin-only RLS for everything** (HR data — staff must not read each other's or their own pay history). Private `staff-review-docs` storage bucket (mirrors vdr-reports posture; per-staff paths `<user_id>/<ts>_<filename>`; signed-URL viewing). Client (`time-tracker.html`): per-staff **📋 Reviews** button in Settings opens an inline panel — history list (date · RAISE/REVIEW chip · `$old → $new /hr\|/period` · note · 📎 Doc · 🗑 delete-with-confirm, delete is history-cleanup only and never reverts the live rate) + add-entry form (Type dropdown Raise/Review-only, effective date, new rate/salary, note, file upload). Recording a raise ALSO updates `staff_config.hourly_rate`/`salary_per_period` so history and live rate never drift; plain rate edits via the Settings Save button are **auto-logged** as raises (compares cached pre-save values; non-fatal if table missing). Idempotent. |
| `40_staff_pay_categories.sql` | Office-staff pay categories. `staff_config` gains `pay_category TEXT NOT NULL DEFAULT 'wise_hourly'` (CHECK `wise_hourly` \| `hourly_bill` \| `salary_dd`) + `salary_per_period NUMERIC(10,2) DEFAULT 0` (CHECK >= 0). Three real arrangements: **wise_hourly** (PH staff — hours × rate via the admin-sent Wise batch), **hourly_bill** (US hourly, e.g. office clerk — portal submits HOURS, accounting computes the pay in QB as a Contract Labor Bill; portal shows hours×rate when `hourly_rate` is set to match QB, hours-only when 0), **salary_dd** (US salaried — fixed `salary_per_period` via direct deposit, hours not used for pay). One-shot backfill: rows still at the default with `pay_type='us'` → `hourly_bill` (re-run never undoes an admin's `salary_dd` flip). `pay_type` kept in sync by the Time Tracker Settings save (wise_hourly→philippines, else us) for legacy consumers (Wise identity fields, PH/US filters). Consumed by Time Tracker `computeStaff` (salary staff earn salary regardless of hours; Pay Period chips 🇵🇭 Wise / 🇺🇸 QB Bill / 🇺🇸 Salary DD; hours input replaced by "— salary" for salaried) and the Finance payroll-run office block (salary rows show "salary"; rate-unset hourly_bill rows show "submit N h"). Idempotent. |
| `39_strata_medb_pdf_storage.sql` | Stores the **source Strata Med B PDF** for reference / audit trail. Creates a private admin-only `strata-medb-pdfs` storage bucket (mirrors the `vdr-reports` posture from migration 08 — `public:false` + separate admin-gated SELECT and `FOR ALL` write policies on `storage.objects`). Adds `medb_pdf_path TEXT` (nullable) to `clinician_payroll_runs`. DROP/CREATEs `upsert_clinician_payroll_run` adding `p_medb_pdf_path TEXT DEFAULT NULL` as the **last** param — on conflict, `medb_pdf_path = COALESCE(EXCLUDED.medb_pdf_path, existing)` so a save without a re-uploaded PDF preserves the prior one. DROP/CREATEs `list_clinician_payroll_runs` to add `medb_pdf_path` to the return shape (history-row link). `get_clinician_payroll_run` unchanged (SETOF table — column rides along). Client (`finance.html`): the Med B import + retro modals stash the dropped File (`cprPreparedMedb._file` / `cprMedbRetroParsed._file`); `cprApplyMedbRetro` carries it to `cprViewState._pendingMedbPdfFile`; both save paths (`saveCprRun` import + `cprSaveRun` Save Adjustments) call `cprUploadMedbPdf(period, file)` to upload to the deterministic per-period path `medb-<period_start>.pdf` (`upsert:true` → replace on re-import) BEFORE the RPC, passing the path as `p_medb_pdf_path`. Upload is **non-fatal** — a storage error logs a warning and still saves the numbers (NULL path → COALESCE keeps any prior PDF). A `📄 View source PDF` link (audit detail footer + history row) opens the stored file via `cprOpenMedbPdf(path)` → `createSignedUrl(..., 60)` (private bucket). `deleteCprRun` removes the stored PDF (best-effort) when the run is deleted. Reference-only — the PDF is never re-parsed. Idempotent. |
| `38_clinician_rates_eval_split.sql` | Extends migration 35's `clinician_medb_rates` with an `eval_rate NUMERIC(8,2)` column (nullable; CHECK eval_rate IS NULL OR >= 0). PT/OT supervisors bill at TWO Med B rates — a Follow-up rate (PTF/OTF) and a higher Eval rate (PTE/OTE). PT/OT assistants (PTA/OTA) still bill at one rate; eval_rate stays NULL for them. Widened `source` CHECK constraint to also accept `'derived_supervisor_split'` for rows whose rate + eval_rate were BOTH derived from TB's per-visit pay column (lowest non-zero distinct = follow-up; highest distinct = eval). RPCs `upsert_clinician_medb_rates(p_rows JSONB)` + `list_clinician_medb_rates()` both DROP-then-CREATE to carry `eval_rate` through (input shape grows the optional field; return-row gets a new column). Manual-override preservation rule unchanged: any row with `source='manual_override'` is preserved against subsequent fresh-derivation upserts. `lookup_clinician_medb_rate` unchanged (single-rate signature; not called from client today). Pre-migration single-rate rows have `eval_rate = NULL` and continue working — client-side `computeMedbForClin()` falls back to the legacy `visits × rate` formula when eval_rate is NULL. Idempotent. |
| `36_clinician_medb_name_aliases.sql` | Learn-as-you-go memory for **Strata-name → TB-clinician** aliases. Strata's productivity PDF sometimes uses slightly different spellings than TB's Prepare-payroll file (e.g. `Gina M Pelehac (OT)` in Strata vs `Gina Pelehac OT` in TB) → the lookup-key match fails and the row goes unmatched. Admin picks the right TB clinician from a per-unmatched-row dropdown in the import preview / retro modal → the mapping persists forever. Table `clinician_medb_name_aliases` keyed by `strata_name_normalized` UNIQUE; carries `strata_name_display` (last-seen Strata display), `tb_lookup_key` (target), `tb_display` (TB display), `hit_count`, `first_seen_at`, `last_seen_at`, `last_set_by`. Three admin-only SECURITY DEFINER RPCs: `list_clinician_medb_name_aliases()` bulk-read for client-side map building on import; `upsert_clinician_medb_name_aliases(p_rows JSONB)` bulk-upserts (input shape: `[{strata_normalized, strata_display, tb_lookup_key, tb_display}, …]`) with hit_count bump + actor stamp on conflict; `forget_clinician_medb_name_alias(p_strata_normalized)` hard-delete by normalized name (for accidental mis-mappings). Drives the alias dropdown in both the import preview's "unmatched names" warning + the retroactive Add-Strata-Med-B modal. Aliases auto-apply at PDF parse-time so subsequent imports auto-resolve. Mirrors the `bank_match_payer_routing` (migration 32) memory pattern. Idempotent. |
| `32_payer_routing_memory.sql` | Adds `bank_match_payer_routing` table (one row per normalized payer name with gl_account / hit_count / first_seen_at / last_seen_at / last_set_by) + `normalize_payer_name(name)` IMMUTABLE helper (lowercase / strip punct + Inc/LLC/HMO/PPO/of-state suffixes / collapse whitespace) + 4 admin-only SECURITY DEFINER RPCs: `lookup_payer_routing(payer)` returns saved GL or NULL, `remember_payer_routing(payer, gl)` upserts with hit-count bump + ON CONFLICT DO UPDATE so most-recent correction wins, `forget_payer_routing(payer)` deletes by normalized name, `list_payer_routings(limit)` for future admin-inspect UI. Backs the learn-as-you-go memory in the Bank Match check scanner — every Add-to-batch fires `remember_payer_routing` so next time the same payer is scanned, the tool auto-applies the saved GL (winning over the static CARRIER_KEYWORDS keyword list). Idempotent. |
| `31_patient_check_deposits.sql` | Adds `patient_check_deposits` table (one row per AR-routed scanned check: payer_name / amount / check_date / check_number / memo / gl_account / category="patient" / scanned_at / scanned_by / strata_cleared boolean + audit / notes) + 5 admin-only SECURITY DEFINER RPCs: `insert_patient_check_deposit(...)` (called by Bank Match `addScannedRow` when GL=AR), `list_patient_check_deposits(only_pending, search, limit)` (TYPE-cast-safe LEFT JOIN auth.users for email; supports pending-only filter + payer/memo/check# search), `mark_patient_check_cleared(id, cleared)` (stamps timestamp + actor), `update_patient_check_memo(id, memo)` (inline-editable memo with NULLIF+TRIM normalization), `update_patient_check_notes(id, notes)` (free-form notes RPC, no UI yet), `delete_patient_check_deposit(id)` (hard delete with client-side two-click confirm). Carrier-routed scans + Authorize.net cc rows deliberately excluded (Strata auto-clears those). Drives the ③ Patient Deposit History card in QB Tools — gives the user a tracker for which scanned patient checks they still need to clear manually in Strata EMR. Idempotent. RLS: admin-only read + write. |
| `30_agency_contracts.sql` | Creates the new `home_health_agency_contracts` sister table (one row per `home_health_agencies.id`, FK ON DELETE CASCADE, UNIQUE on agency_id for v1) — per-discipline rates (`rate_ot_eval`/`rate_ot_assistant`/`rate_pt_eval`/`rate_pt_assistant`/`rate_st_eval`/`rate_st_other` matching the user's HH RATINGS xlsx codes OTF/OTFA/PTF/PTFA/STE/STEO), 1–5 ratings (`rating_payment`/`rating_communication` with CHECK constraints), contract metadata (`contract_location`/`is_active`/`contract_start_year`), payment workflow (`preferred_payment_method`/`sent_via`/`collections_contact`), free-text `notes`, and `created_at`/`updated_at`/`updated_by` audit columns auto-stamped by a trigger. RLS: **admin-only** read + write across the board (matches Finance posture). New `normalize_agency_name(name)` IMMUTABLE helper lowercases, drops punctuation, strips Inc/LLC/Ltd/Corp/Services/Co/PC suffixes, collapses whitespace — used by the import RPC to match incoming names against the existing portal agencies table. Three admin-only SECURITY DEFINER RPCs: (1) `list_agency_contracts()` — LEFT JOIN `home_health_agencies` so all agencies appear (even those without a contract row, rendered as "—" in UI); (2) `upsert_agency_contracts(rows JSONB)` — bulk import; matches inbound rows to agency_id via normalized-name lookup OR manual agency_id override, returns `{matched: INT, unmatched: [{name}]}` so UI can render "M of N matched"; (3) `update_agency_contract(p_agency_id, p_field, p_value JSONB)` — single-row field-level patch used by inline editors; whitelisted field list, type-coerces JSONB scalar into the right column type (NUMERIC/SMALLINT/BOOLEAN/TEXT) via dynamic SQL with type-safe USING binding. Auto-creates the contract row on first edit so the UI doesn't need separate insert/update paths. Drives the new `agencies.html` tool. Idempotent. |
| `29_eval_auto_complete_on_sync.sql` | When `apply_eval_visits` flips `sync_status` to `'synced'`, also auto-promotes `category` to `'Completed'` — fixes the gap where rows stayed in Issues / To Sync / No Sched / Scheduled-Late / Re-Eval / Assigned even after the eval was done and synced. **Hold/Hospital** is preserved (patient off-service; the sync may be from a prior eval still on file). Includes a one-shot backfill that promotes existing `sync_status='synced'` rows to `'Completed'` for any non-Hold/Hospital category, so the existing backlog clears immediately. Replaces the migration-28 RPC body with the category-aware version. Idempotent. |
| `28_eval_visits_episode_match.sql` | Bug fix — `apply_eval_visits` was matching only on `(patient_id, service)`, so for returning patients (multiple referrals over time), the eval visit from a prior episode would attach to the current row (e.g. "PTE done 4/8" showing on a row whose referral date was 6/1). Replaces the RPC with an episode-aware version that also matches on `episode_start_date`. Includes a one-shot cleanup that NULLs out `eval_visit_*` / `reeval_visit_*` fields and resets `sync_status` to `'no_evaluation'` for any row where the previously-attached eval visit date falls before its own `episode_start_date` (provably from a prior episode). Companion JS change in `compliance.html` adds `epstart` to `EVAL_VISITS_REQUIRED_COLS`, buckets the Patient Visits aggregator by `(patient_id, service, episode_start_date)`, and carries `episode_start_date` into each row passed to the RPC. Idempotent. |
| `27_eval_proposed.sql` | Adds 7 columns to `evaluation_compliance_items` for the **Proposed Eval** column (`proposed_eval_date` / `proposed_eval_clinician` / `proposed_eval_note` / `proposed_eval_set_at` / `proposed_eval_set_by` / `last_acknowledged_at` / `last_acknowledged_by`) + creates a separate `evaluation_proposed_history` audit table (action CHECK over `proposed`/`updated`/`confirmed`/`drift_acknowledged`/`cleared`). Four SECURITY DEFINER RPCs route all writes: `set_proposed_eval(item_id, date, clinician, note)`, `acknowledge_proposed_eval(item_id, action)`, `clear_proposed_eval(item_id)`, `list_proposed_history(item_id)`. RLS: read for any authenticated; direct writes blocked (all changes flow through RPCs). Drives the new "Proposed Eval" column in `compliance.html` — employee enters a planned date, portal compares against TB's `next_eval_date` (from migration 15) and renders one of four states (Empty / Awaiting / Confirmed / Drift). Confirmation/drift-ack history rows are written only on explicit click (no spam from background imports). Idempotent. |
| `26_clinician_llc.sql` | Adds two nullable columns to `clinician_profiles`: `llc_name TEXT` (business-entity name — e.g. "Rafael Therapy Services LLC", the payable-to on the QB Bill) + `llc_ein TEXT` (EIN, free text so admin can paste however accounting tracks it). Partial index on `llc_name` (NULLs excluded) supports future Phase 3 "group by payable-to entity" queries. Admin enters both from Roster Review; LLC name surfaces as a 🏢 sub-line on the Map profile header, the Finance Clinician Payroll Audit per-clinician rows, and the Payroll Overview reference card. EIN displays only in Roster Review (admin reference for 1099 / vendor setup). Idempotent. |
| `25_clinician_payment_two_axis.sql` | Splits clinician payment into two orthogonal axes — `payment_method` (now `'bill' \| 'w2'`, billing type for QuickBooks) + new `payment_delivery` (`'paper_check' \| 'direct_deposit' \| 'zelle'`, delivery mechanism). One-shot data migration translates legacy single values (`check`/`direct_deposit`/`zelle`/`w2`) into the (bill_type, delivery) pair; admin can fine-tune from Roster Review afterward. New `payment_delivery` column has a CHECK constraint over the 3 allowed values, default `'paper_check'`, plus an index for Phase 3 export grouping. `payment_method`'s CHECK is tightened to `('bill','w2')` and its default reset to `'bill'`. Idempotent. |
| `24_staff_birthday_vacation_alerts.sql` | Adds `dob DATE` to `staff_config` + a month*100+day index, plus two SECURITY DEFINER RPCs that surface on the Home page 📰 News & Alerts board: `list_staff_birthday_alerts()` (active staff with birthday today or tomorrow — admin enters via Time Tracker Settings) and `list_staff_vacation_alerts()` (groups consecutive `absence_kind = 'vacation'` days from `time_entries` into vacation blocks via gap-and-island SQL, returns blocks that include today or start within the next 14 days). Alerts share the existing `ALERT_META` / `buildAlertMessage` rendering shape — staff variants use `staff_id` / `staff_name` / `staff_role` instead of `clinician_*` and click-through goes to the Time tab. Idempotent. |
| `23_time_entry_absences.sql` | Adds `absence_kind TEXT` column to `time_entries` with CHECK constraint enforcing 5 allowed values (`vacation`/`sick`/`personal`/`power_outage`/`internet`) + index on the column for per-period absence summaries. Drives the new **Day Off** dropdown on each row of the staff My Timesheet card (🏖 Vacation / 🤒 Sick / 👤 Personal / ⚡ Power Outage / 📶 Internet). Absence-only days store as a zero-duration `is_manual` time_entry (`clock_in == clock_out`) so payroll totals are unaffected — the reason is purely informational. Partial-day absences are supported (hours > 0 plus an absence reason). Admin Shifts modal renders an amber chip next to the Type column when `absence_kind` is set. Idempotent. |
| `22_pay_period_submissions.sql` | Creates `pay_period_submissions` (one row per (user_id, period_start) with a 4-state machine: `draft` → `submitted` → `approved` / `rejected`) + 4 SECURITY DEFINER RPCs (`submit_pay_period`, `list_pay_period_submissions`, `approve_pay_period`, `reject_pay_period`). RLS: staff read own + admin reads all; writes routed through RPCs. Drives the per-period submission badge on the staff My Timesheet card and the admin's Approve/Send-back actions on Dashboard cards, Pay Period rows, and the Shifts modal. Approval is a stamp (not a hard lock — admin can still edit). Idempotent. |
| `21_reimbursement_receipts.sql` | Adds 5 columns to `pay_period_adjustments` (`kind` bonus/deduction/reimbursement, `receipt_url`, `original_amount`, `original_currency`, `fx_rate`) + creates the public-read `reimbursement-receipts` storage bucket (admin-write, mirrors the `staff-photos` policy pattern). Drives the Time Tracker → Pay Period reimbursement flow: a Reimbursement adjustment type with a PHP/USD currency toggle + live PHP→USD conversion (table still stores USD in `amount`; original peso amount + FX rate preserved for audit) and an optional receipt upload surfaced as a 📎 link on the adjustment's sendable sub-row. Idempotent. |
| `20_clinician_payment_method.sql` | Adds `payment_method TEXT NOT NULL DEFAULT 'check'` column to `clinician_profiles` with CHECK constraint enforcing 4 values: `'check'` (default — accounting creates QB bill, user generates check), `'direct_deposit'` (bank ACH), `'zelle'` (P2P transfer), `'w2'` (QB Payroll). Index on the column for export-grouping queries. Drives the Roster Review "Pay Method" column (default hidden, opt-in via Columns ▾) — inline 4-option dropdown writes here. Phase 3 (export to accounting) reads this column to split the audited payroll payload into per-method export files (QB bill IIF / ACH file / Zelle manual list / QB Payroll import). Idempotent. |

### Mapbox
- **Access token:** `pk.eyJ1IjoiZGl6dG9ueTY3IiwiYSI6ImNtbjVjNW1seTA4dWsycXBpbjRreHVoOHQifQ.7wgw3ocLrvjEmpKdx-vP1A`
- **Used in:** clinician-map.html (v3.0.1), referrals.html (v3.3.0)

---

## Shared Supabase Tables

| Table | Used By | Notes |
|---|---|---|
| `clinician_v2` | clinician-map | |
| `clinician_profiles` | clinician-map, referrals (audit) | |
| `clinician_zip_coverages` | clinician-map | |
| `home_health_agencies` | clinician-map, referrals | |
| `referrals` | clinician-map (overlay), referrals, kanban (linked entity) | |
| `referral_contacts` | clinician-map (overlay), referrals | |
| `staff_config` | time-tracker, kanban, chat | Now has `display_name`, `display_color`, `photo_url` columns used portal-wide for the staff directory |
| `time_entries` | time-tracker | |
| `time_edit_requests` | time-tracker | |
| `user_roles` | all files | Drives admin gates (board management, staff profiles, etc.) |
| `language_options` | clinician-map | |
| `therapy_boss_*` | clinician-map (bulk import) | |
| `boards` | kanban | Top-level workflows; admin-edit only |
| `board_columns` | kanban | Per-board columns; admin-edit only |
| `tasks` | kanban | Tasks; all auth users can CRUD; can link to clinician/referral/compliance_item/agency |
| `chat_messages` | chat-widget.js (all pages) | Single team channel; SELECT for all auth users; INSERT only as self |
| `evaluation_compliance_items` | compliance.html (Evaluation tile) | One row per (patient_id, referral_id) anchored to TB Active Services; portal-managed `category`/`sync_status`/`status_notes`/`clinician_id`; admin+editor write |
| `clinician_visit_period_metrics` | roster-review.html | One row per (clinician_name, pay_period_start) — pre-aggregated visit counts + avg sync days + avg duration. Populated by the Eval Compliance Patient Visits import (same file, two consumers). Read by Roster via `list_clinician_visit_summary()` which mirrors the VDR summary shape so the cell renderer is source-agnostic. |

### Database functions (RPCs)
| RPC | Purpose | Caller |
|---|---|---|
| `list_staff()` | Returns user_id, email, display_name, display_color, photo_url, role for all users. SECURITY DEFINER bypasses auth.users RLS. | kanban, chat-widget |
| `upsert_staff_profile(user_id, name, color, photo_url)` | Admin-only — sets display fields. NULL leaves a field unchanged. | kanban Manage Staff modal |
| `clear_staff_field(user_id, field)` | Admin-only — explicitly NULLs `display_name` / `display_color` / `photo_url` | kanban Manage Staff modal |

---

## User Roles

Three roles stored in `user_roles` (user_id, role):
- **admin** — full access including user management, bulk import, settings
- **editor** — can edit clinicians and referrals, cannot manage users
- **readonly** — view only (default for new signups)

**Default-on-signup**: Since `05_auto_user_role.sql` and `06_auto_staff_config.sql`, every new account gets a `user_roles` row (`readonly`) AND a `staff_config` row (`is_active=false`) created automatically. Admins promote via:
- **Map → 👥 Users** modal (admin only) — reads via `list_staff()` RPC so even brand-new orphans appear immediately; the role dropdown does an upsert so promoting an un-roled user works
- **Time Tracker → Dashboard** — un-set-up staff appear under a yellow "⚠ N new users need setup" banner with a **Set up** button that jumps to the Settings tab and highlights the row
- **Time Tracker → Settings** — every staff name is now an editable input that calls `upsert_staff_profile` RPC; admin can rename + set rate + flip Active in one save

---

## How to Start a Session

1. Read this file (`CLAUDE.md`) for project overview
2. Read the specific CLAUDE file for the area you're working on (e.g., `CLAUDE-map.md`)
3. Read only the relevant section of the HTML file using line offsets if needed

**Always update the relevant CLAUDE file at the end of the session** with any new functions, schema changes, or completed features. Ask: "Update the CLAUDE file with what we built today."

---

## Pending Work (as of June 11, 2026)

### Done since last update
- ✅ **Map: Bulk Import — fix Clinician Import Mode showing for non-clinician types** (`clinician-map.html`, no SQL — CSS bug) — the mode shell is `<div class="field" hidden>` toggled by `syncImportKindUi` (`hidden = importKind !== "clinician_master"`), but `.field { display: grid }` (line 734) overrode the `[hidden]` attribute's default `display:none`, so the Additions Only / Sync Update / Full Initial Load buttons stayed visible under HH Agencies / ZIP Coverage / Completed Services. Added `.field[hidden] { display: none !important; }` (id+attr out-specifies `.field`) so any hidden `.field` actually hides. Now the mode selector appears only for Clinicians, as intended.
- ✅ **Map: Bulk Import — field mapping is admin-only; editors get an auto-handled 3-step flow** (`clinician-map.html`, no SQL) — per the user, TherapyBoss exports always carry all fields and the auto-matcher (`guessImportFieldMap`) already grabs what it needs, so the per-column mapping dropdowns are a power-user override, not an everyday step. **(1)** The "Check the columns" card (`<section class="import-card admin-only">` wrapping `#import-mapping-grid`) + its step-strip `<li>` are now `admin-only` — editors don't see them at all. Auto-map still runs on file load and fills `appState.importFieldMap` independent of the UI, and `uploadImportToSupabase`/per-kind uploaders read that state (not the DOM), so editor preview + upload are unchanged. Admins keep the override dropdowns (card heading carries the red `admin` tag + reworded as an optional override). **(2)** `syncImportSteps()` renumbers the **visible** step badges by role so editors read **1 Choose · 2 Pick the file · 3 Preview & upload** (admins still 1-4); `applyRoleMode` re-calls it once the role resolves so an admin never sees a stale 1-2-3-3. **(3)** Dropped the hardcoded `Step N · ` prefixes from the card headings/labels — the numbered strip is now the single source of step ordering (kills the 1/2/4 mismatch when step 3 is hidden). **(4)** Help chips relabeled "Pulled in automatically — the importer grabs what it needs from your file" (no more "skim Step 3"). **(5)** Editor safety net: if the auto-matcher recognizes nothing (`getImportMappedFieldCount()===0`) the footer shows an amber "couldn't recognize this file's columns — re-export from TherapyBoss or ask an admin" message instead of a silently-disabled Upload button. **(6)** Per-type **"What happens when you upload"** line added to the help block (`renderImportKindHelp` reuses the per-kind, mode-aware `getImportFooterMessage`) so each type states its one fixed behavior right at the picker (HH Agencies = create-missing + update-matched; ZIP Coverage = replace each matched clinician's ZIPs; Completed Services = append new visit rows, skip loaded; Clinicians = follows the selected mode). The **mode selector stays clinician-only** — confirmed it's the one type where the map is source-of-truth and overwrite-vs-skip is a real choice; the others each do one sensible thing, so no mode toggle for them.
- ✅ **Map: Bulk Import — TherapyBoss export steps for Clinicians/ZIP** (`clinician-map.html`, no SQL) — per-type help "Where to get the file" now gives the real TB workflow: Clinicians tab → set Status filter to **All** → green Excel icon → choose **Clinicians** (or **Zip coverage**). TB exports every column — nothing to pick.
- ✅ **Map: Bulk Import — Full Initial Load gated to admin + mode-scope clarity** (`clinician-map.html`, no SQL — follow-up to the editor-friendly pass) — Full Initial Load can overwrite the entire roster, so it's now **admin-only**: the button carries `admin-only` (hidden for editors via `body.editor-mode .admin-only`) + an `admin` tag chip, and two JS backstops bounce a non-admin off the mode — `setClinicianImportMode` forces `full_initial_load`→`additions_only` for non-admins, and `uploadImportToSupabase` blocks + resets if it somehow slips through. Editors keep **Additions Only** (safe default) + **Sync Update**. Also clarified that the modes are clinician-specific: the "Clinician Import Mode" label now reads "— applies to Clinician imports only" (the selector already only renders for the Clinicians type).
- ✅ **Map: Bulk Import made editor-friendly** (`clinician-map.html`, no SQL) — editors can now run imports without confusion. **(1) Access**: dropped `admin-only` from the `#toolbar-bulk-import` button (kept `edit-only`) so editors + admins see it, readonly doesn't; no JS gate on open/upload (the upload button was already `edit-only`, and Phase-1 RLS is authenticated-all so editors can write). **(2) Guided steps**: a numbered `#import-steps` strip (① Choose what you're importing → ② Pick the file → ③ Check the columns → ④ Preview & upload) with the active step highlighted live via `syncImportSteps()` (driven by file-loaded + mapped-field state from `syncImportUploadState`). **(3) Plain-English copy**: rewrote `getImportKindDescription` / `getClinicianImportModeDescription` / `getImportFooterMessage` + the static card headings/intros to drop jargon (`clinician_v2`, "upsert") and read as staff instructions; cards now titled "Step 1 · What are you importing?" etc. **(4) Per-type help** (`#import-kind-help`, `renderImportKindHelp()` from an `IMPORT_KIND_HELP` config keyed by kind): shows "where to get the file" (which TB report) + expected-column chips generated from `getActiveImportFields()` so they always match the mapping dropdowns; Completed Services calls out the combined-or-split date-column note + points at admin 🛠 Fix Dates. **(5) Soft nudge**: `getImportRecommendedNudge()` adds a non-blocking amber footer hint when Completed Services is loaded without Clinician Names mapped (matches would be empty) — never blocks upload. All re-renders ride the existing `syncImportKindUi` / `syncImportUploadState` hooks.
- ✅ **Map: completed-services split Year/Month/Day date import + in-place repair** (`clinician-map.html`, no SQL) — root cause of the 07/17/1905 (and later 01/01/202X) auto start/hire dates. A TherapyBoss "Completed services" export delivered dates ONLY as split columns (`Referral date.Year`=2026 / `.Month`="January" / `.Day`=2) with the combined `Referral date (MM/DD/YYYY)` column **blank** (verified: 0/649 combined vs 648/649 split). The importer's substring auto-matcher bound `referral_date`→`Referral date.Year` and fed the bare year to the date parser → 1905 (pre-#128 serial) or 2026-01-01 (`new Date("2026")`). Fix (all in clinician-map.html): **(1)** `parseMonthToken` + `reconstructImportDateFromParts(rawRow, baseLabel)` (scans for `<base> year/month/day` headers) + `resolveCompletedServiceDate` (combined column wins, else reconstruct from splits) wired into the completed_services row builder for all 4 date fields. **(2)** bare-4-digit-year guard in `normalizeImportDateValue` (a lone "2026" → null, never a fabricated Jan-1). **(3)** `guessImportFieldMap` skips `.year/.month/.day` headers for date fields so they never auto-bind. **(4)** Service-Years filter floored at ≥2000 so a residual 1905 row can't render a bogus year button. **(5)** Admin-only **🛠 Fix Dates** toolbar button → `repairCompletedServiceDates()`: pages every `therapy_boss_completed_service_import_rows`, recomputes the 4 dates from each row's stored `raw_payload` (combined-or-splits), batch-updates changed rows, re-derives each clinician's earliest plausible date via the `therapy_boss_completed_service_import_clinicians` link, and writes `tags.derivedStartDate` directly (overrides a bogus stored value even when the corrected date is later; explicit `start_date` untouched). Idempotent (clean re-run = 0 updates); reports rows/clinicians touched + sample earliest dates to the console + a toast. No re-export needed — `raw_payload` preserves the original split cells.
- ✅ **Clinician Payroll Audit — per-clinician Med B breakdown (📋 column)** (`finance.html`, no SQL) — therapists sometimes request a breakdown of their Med B visits; Strata's per-clinician export ("Staff Productivity - Adam Ouyang (PT)") carries Patient / Service Date / Submission Date / **Payer** / Unit Breakdown / Units / Minutes / **Total Amount**, and the admin used to hand-delete the two bolded columns before sending. New narrow **📋 column** on the audit per-clinician table: click → pick the clinician's PDF → `cprParseClinBreakdownPdf` (PDF.js Y-band state machine: date-pair anchor lines, wrapped-name fragments, continuation CPT tokens, bare-int units/minutes bands; payer + $ parsed past and **discarded**) → wrong-file guard via `cprNameSimilarity` against the row's clinician (confirm, not block) → **print-ready cleaned view** opens (Patient / Service Date / Submitted / Unit Breakdown / Units / Minutes + totals row — no payer, no dollars) and auto-fires the print dialog (admin prints or Save-as-PDF). **Auto-flags on print**: `breakdown_sent_at/_by/_filename` stamped into the clinician's `per_clinician_payload` entry (additive JSONB — persists via Save Adjustments, zero schema work); row shows a green `📋 ✓ M/D` chip + ↻ reload + ✕ clear (confirm); header strip shows "📋 N breakdowns sent". Optional per clinician, gated off `status==='open'` like all edit affordances. PDF parse-and-discard (no storage) per user decision. Totals row sums visits + units only — **minutes deliberately not totaled** (per-visit minutes stay informational; a summed figure invites hours-worked readings the breakdown isn't meant to support).
- ✅ **Strata Med B — provably-complete import: ordered-bijection parser + reconcile-to-printed-total + verify-and-correct UI** (`finance.html`, no SQL — PRs #102–#111, #115, #117 arc) — the "100% accuracy" workstream. **(1) Ordered-bijection parser** (#115, the breakthrough): Strata sorts staff cards by Visits Billed descending and stacks them vertically, so the Nth staff name in document order owns the Nth data row in document order — `cprParseStrataPdf` now collects names + data rows as ordered lists and zips them, replacing the fragile proximity search (#105–#107 heuristics kept verbatim as fallback). Four validation gates, ALL must hold or it falls back: names==rows count, visit counts non-increasing (sort intact), row sum == printed Combined-Staff total, no duplicate keys. Proven on a real export by CPT semantics (97537→the OT's card; 97163 eval + $390 → a PT card) and exact dollar arithmetic (8×$240+1×$390 = the printed $2,310; all rows = the printed $12,780). Eval attribution became per-card (own data row → next data row, capped at the Combined-Staff line) fixing both a missed eval (outside the old name-anchored scan) and a phantom eval (totals-section bleed into the last clinician). `[^\d$]` fix in both CPT regexes so "97530 x $0.00" can't read as 97530×0. **(2) Reconcile gate** (#110): `extractStrataTotals` reads the printed Combined-Staff **Visits Billed** integer (dollar total deliberately ignored — unreliable per admin); retro preview shows a reconciliation banner (green match / red short with per-orphan-row hints / amber over) and **Apply hard-blocks until assigned == printed** (or explicit "Apply anyway" bypass). Status pill + 🔎 parser trace show the printed total + `ASSIGNMENT MODE`. **(3) Verify-and-correct UI** (#108): per-row ✓ checkbox (Apply gated until all ticked; "✓ Verify all" button; mint-green when ticked), **editable Vis + Eval number cells** (recompute Med B live, eval clamps ≤ visits), and a **＋Add missing clinician** roster picker footer row (yellow 'manual' chip + ✕ remove; default 1 visit) for rows the PDF genuinely lost. **(4) Checking ergonomics**: preview rows render in **PDF document order** (#117 — stable-sorted by first appearance in parsed rows; non-PDF rows sink to bottom) + a 🔍 **type-to-isolate name filter** (display-toggling only — no re-render, no focus loss, math untouched; Esc clears; persists across re-renders); sticky table headers on scroll (#111); modal widened 720→1080px (#109); in-UI **🔎 parser trace** (#104 — parsed names/visits, assignment mode, printed-total reconcile state, orphan rows, all reconstructed PDF.js lines) so future parser regressions are debugged from ground truth instead of guesses.
- ✅ **Strata Med B — smart unmatched-name suggester + alias confirm/undo + date-range guard** (`finance.html`, no SQL) — three import-safety layers: **(1) Fuzzy suggester** (#110): new `cprNameSimilarity` (token-set Jaccard weighted 0.65 + normalized Levenshtein 0.35 over `cprClinLookupKey`-normalized names — first scorer in the codebase) + `cprSuggestMatch`; unmatched Strata names get their best roster/TB match **pre-selected** in the dropdown with a `💡 Suggested: <name> (NN% match)` hint + one-click **✓ Confirm** when score ≥ 0.6 (below threshold → stays on Skip, manual search). **(2) Confirm/undo** (#99): picking a mapping no longer silently vanishes the row — it stays visible with a green `✓ Mapped to: <name>` badge + **✕ Undo** until Apply; headline counts `⚠ N unmatched · ✓ N mapped`. Both retro + import modals. **(3) Date-range guard** (#101): `extractStrataDateRange` reads the PDF's Date Range header; both modals show a banner — green when it equals the pay period, loud yellow mismatch (wider/narrower/different diagnosis) when not, grey when unreadable; status pills echo the parsed range. Also: Med B ✎ edit button on the audit row was invisible (CSS reveal rule only matched the visits table) — now always visible on the Med B cell (#100).
- ✅ **Strata Med B — source PDF stored for audit trail** (`finance.html` + `supabase/policies/39_strata_medb_pdf_storage.sql`, #114) — see the migration-39 row above for full detail. Private admin-only `strata-medb-pdfs` bucket; both save paths auto-upload the dropped PDF to `medb-<period_start>.pdf` (replace on re-import; non-fatal on storage error); `📄 View source PDF` link on audit detail + history row via signed URL; delete-run cleans up the object; saving without a re-upload preserves the prior PDF (COALESCE).
- ✅ **Strata Med B — reopen shows the full editable breakdown** (`finance.html`, no SQL — #112/#113) — after Apply + Save, re-opening 🩺 Update Strata Med B used to show a blank dropzone (the parse was discarded). Now: **apply stores the derivation** (`medb_visits` / `medb_eval` / `medb_rate` / `medb_eval_rate` per clinician inside `per_clinician_payload` — additive JSONB) and **`cprSeedMedbRetroFromApplied` rebuilds the full editable preview** on open — same Vis/Eval/F-up/rates columns + the `N × $rate` math sub-line showing exactly how each amount was achieved; edit anything and re-Apply. Seeded rows are pre-verified; date-range/reconcile banners suppressed (no PDF → blue "currently applied" note instead); dropping a fresh PDF still fully replaces (resets verify state so seeded pre-verify can't leak). Backward-compat: pre-derivation audits reconstruct visits as `medb_total ÷ rate` (exact for single-rate assistants, correctable for split-rate supervisors, exact after the next Apply).
- ✅ **Med B eval codes — complete CPT coverage + credential-variant aliases** (`finance.html`, no SQL — #116) — modern eval sets were already complete (PT 97161-97164, OT 97165-97168); added the deleted pre-2017 codes (97001/97002 PT, 97003/97004 OT) as zero-collision-risk insurance; 97169-97172 (athletic trainer) deliberately excluded. The real find: **DPT / OTR / COTA credential variants** parsed fine but fell through four exact-match `"PT"`/`"OT"`/`"OTA"` checks — `cprEvalCodesFor` (DPT's evals silently counted as follow-ups → underpaid), `cprIsPtOrOtSupervisor` (no split-rate), Pay Rates importer routing (skip-out-of-scope), and the Pay Rates card roster filter (invisible). All four now alias DPT→PT, OTR→OT, COTA→OTA, with kept-in-sync cross-reference comments.
- ✅ **Clinician Payroll Audit — split PT/OT supervisor rates into Eval + Follow-up** (`finance.html` + `supabase/policies/38_clinician_rates_eval_split.sql`) — admin flagged that PT and OT supervisors don't bill at a single per-visit rate. They have TWO rates: an Eval rate (PTE/OTE) for evaluation visits and a Follow-up rate (PTF/OTF) for treatment visits. PT/OT assistants (PTA/OTA) keep one rate (PTFA/OTFA — assistants don't bill evals). ST/SLP are out of scope (no Med B in this shop). **Migration 38** extends `clinician_medb_rates` with an `eval_rate NUMERIC(8,2)` column (nullable); widens the `source` CHECK to add `'derived_supervisor_split'`; DROP/CREATEs the `upsert_clinician_medb_rates` + `list_clinician_medb_rates` RPCs to carry eval_rate. Backward compatible — pre-migration rows have `eval_rate=NULL` and continue using the legacy single-rate compute. **PDF parser** (`cprParseStrataPdf`): after capturing each clinician's name + visits, scans the Unit Breakdown text slice (from the name line forward to the next staff-name boundary) for CPT eval codes — PT: 97161/97162/97163/97164; OT: 97165/97166/97167/97168 — and sums their counts into a new `eval_count` field per row. Output shape grows `eval_count` + `follow_up_count`. **Rate derivation** (`cprDeriveRateForClinician`): now returns `{rate, eval_rate, source}`. PT/OT supervisors with two distinct TB pay values → `rate = lowest_non_zero`, `eval_rate = highest_distinct`, source = `derived_supervisor_split`. Single-value periods fall back to `derived_supervisor_min` with `eval_rate=null`. Assistants unchanged (`derived_assistant_mode`, single rate). **Med B compute** (`computeMedbForClin` shared helper): when `eval_rate` is provided AND `eval_count > 0`, uses `eval_count × eval_rate + (visits - eval_count) × rate`; otherwise falls back to `visits × rate`. Wired into `mergePayAndMedb` + `cprApplyMedbRetro`. **New 💵 Pay Rates card** in the Payroll sub-view (admin-only, collapsible): lists every PT/PTA/OT/OTA clinician with their stored rates; inline-editable Follow-up + Eval cells (Eval disabled for assistants with an italic em-dash placeholder); search + discipline filter + source filter; saves debounced on blur/Enter via the existing upsert RPC with `source='manual_override'`. **📥 Import Pay Rates modal**: drops the Strata `Pay rates.xlsx` (auto-detects columns Name/PTF/PTE/OTF/OTE/PTFA/OTFA — case-insensitive); matches each row's Name against `cprRoster`; renders a preview with per-clinician current-vs-new rates + a yellow warning strip for unmatched names / no-rate rows / out-of-scope disciplines; Apply bulk-upserts all writable rates as `manual_override`. Drives the full PT/OT eval-split workflow: drop the rate sheet once → next Strata PDF import auto-detects eval codes → Med B = eval × PTE + follow-up × PTF lands correctly.
- ✅ **Strata Med B — parser hardening + roster-aware unmatched picker + editable rate** (`finance.html`) — five compounding improvements after the name-alias memory shipped, ironing out the Magdalena Chowaniec workflow: **(1) PDF parser regression-proofed** — `findVisits` now accepts any `^(\d+)(?=\s|$)` candidate (bare integer at start-of-line followed by whitespace/end), covering all three rendering modes observed in real Strata PDFs (per-cell separated / integer+percent merged / full row-Y-band merge). Decimal lines like `4.0 14.8 ...` fail the lookahead naturally; CPT codes like `97110 x` rejected by an explicit `^\d+\s+x\b` pre-check; Unit Breakdown counts rejected by `validateVisitsAt`'s prior-line CPT-tail check. `validateVisitsAt` now passes when a `$X,XXX.XX` line appears either on the SAME line (row-merge) OR within the next 3 lines. Diagnostic `console.warn` logs the first 80 PDF.js lines when the parser returns 0 clinicians so future regressions can be debugged without guessing. **(2) Multi-line name combine** — Strata wraps long names across two lines (`Magdalena\nChowaniec PTA`); a pre-process pass detects a short alphabetic prior line followed by a staff line and prepends them so the name regex captures the full name. **(3) Roster-aware dropdown** — `cprFetchRoster()` pulls `clinician_v2` + `clinician_profiles.status` on every modal open; `cprTbClinicianOptions` appends every roster clinician not already in this audit's TB perClin, tagged `· roster` / `· paused` / `· inactive` in the label. Filters out Inactive by default; an **"Include inactive"** checkbox above the unmatched table reveals them. Covers the Med B-only case where a clinician has Strata activity but no TB visits this period. **(4) Med B-only row creation** — `mergePayAndMedb` + `cprApplyMedbRetro` distinguish three groups of Strata rows: matches-TB → add Med B to existing entry; matches-roster-only → create new `_medb_only: true` entry with `pay_total=0` + `medb_total = visits × rate`; neither → stays in unmatched. Both the audit table and import preview render Med B-only rows with an indigo **"Med B only"** chip next to the name. On re-imports these rows live in `per_clinician_payload` so `tbByKey` includes them and the merge treats them as regular rows. **(5) Editable rate cell in the retro modal preview** — typed value lands in `cprRateEdits` and recomputes New Med B live; flows through `cprApplyMedbRetro` (typed value beats memory/derived); persists as `source='manual_override'` to `clinician_medb_rates` on Save Adjustments. Both retro + import flows exclude roster matches from the unmatched filter so picking a roster clinician immediately moves the row out of the warning strip and into the preview table as a "Med B only · new" row.
- ✅ **Strata Med B — name-alias memory + split-page PDF fix** (`finance.html` + `supabase/policies/36_clinician_medb_name_aliases.sql`) — two compounding improvements: **(1) Inline dropdown picker** for unmatched Strata names. When a Strata row's normalized name doesn't match a TB clinician in this period (e.g. `Gina M Pelehac (OT)` in Strata vs `Gina Pelehac OT` in TB), the warning strip now renders a `<select>` of every TB clinician in this audit per unmatched row. Picking one assigns the alias to `cprAliasPending` + immediately re-applies aliases to the Strata payload + re-renders the preview (so the row pops into the matched table with computed Med B). Surfaces in **both** the import-modal preview AND the in-audit retro modal. Aliases persist to `clinician_medb_name_aliases` (migration 36) via fire-and-forget `cprPersistAliases` AFTER audit-save succeeds — future imports auto-resolve at PDF parse-time. **(2) PDF parser fix for split-page clinician rows.** Refactored both parser paths to a shared `cprParseStrataPdf(arrayBuffer)` helper with proper handling for Strata's multi-page PDFs where a tall row (photo + name + many unit-breakdown lines) gets split across the page boundary. New approach: validate every visits-candidate integer by requiring a `$X,XXX.XX` line within the next 3 lines (Total Billed always follows Visits Billed; Unit Breakdown counts are followed by CPT codes like `97140 x`, so they fail validation). Plus bidirectional search: forward-search from name first (normal case); if forward fails, backward-search up to the prior staff-name boundary (split-row case where the name landed on the NEXT page but data is on the PREVIOUS one). Consumed-index tracking prevents the next clinician's name from accidentally claiming a prior clinician's data line. Both parser paths (import + retro) now share the helper, so future bug fixes apply uniformly.
- ✅ **Strata Med B — retroactive add affordances** (`finance.html`) — two ways to get Med B onto an audit AFTER it was saved without Strata: (1) **🩺 Add / Update Strata Med B button** in the audit footer opens a dedicated modal that takes a Strata PDF only (no TB re-upload required) — reuses the existing PDF parser + rebuilds the rate map from this audit's saved TB visits + memory; preview table shows per-clinician Current Med B vs New Med B with deltas; Apply rewrites every matched clinician's `medb_total = strata_visits × rate` (and zeroes any prior Med B for clinicians NOT in the new Strata file — full replace, same as a re-import), marks the run dirty, recomputes totals, and stashes the rateMap so the subsequent Save Adjustments persists rates to memory. (2) **Inline click-to-edit Med B cell** in the per-clinician audit row — ✎ button opens a number input on the col-medb cell; Enter/✓ commits, Esc/✕ cancels; sets `medb_source='manual'` so the value is treated as a flat per-period override (no rate inference). Both flows leave the period in `dirty` state requiring an explicit Save Adjustments click — same affordance pattern as visit exclude / pay edit / adjustment. Both flows refuse to mutate locked audits — user must Reopen first.
- ✅ **Strata Med B — Phase 2: PDF parser + learned per-clinician rate** (`finance.html` + `supabase/policies/35_clinician_medb_rates.sql`) — replaces the Phase-1 stub parser with a real Strata PDF reader (the Strata "Staff Productivity Report (by Charge Submission)" is PDF-only — Strata has no CSV/XLSX export for this report). PDF.js 3.11.174 (last UMD build; 4.x is ESM-only) loaded from cdnjs; workerSrc set inline right after the script tag. Parser groups text items by Y-coordinate (±3px) into lines, then regex-matches each line for staff names — handles both `"Adam Ouyang (PT)"` (parens) and `"Magdalena Chowaniec PTA"` (no parens) forms, tolerates a trailing comma in the name (e.g. `"Jordan Juralbal, (PT)"` in real data), and finds Visits Billed as the first integer line within 12 lines following the name. Skips the "Combined Staff Totals" footer + page boilerplate. **Per-clinician rate** is derived discipline-aware from this period's TB visits: **assistants** (PTA/OTA/STA/COTA/SLPA/LPN/CNA/HHA) → **MODE** of TB pay values (assistants only do follow-ups → single consistent rate); **supervisors** (PT/OT/ST/SLP/OTR/RN/…) → **LOWEST non-zero distinct** TB pay value (drop the high-rate eval rows since Med B Medicare visits are treatment work). Memory wins over fresh derivation when present (table `clinician_medb_rates`, migration 35); auto-refreshed on every TB import so cached rates stay current as standard rates shift. **Import preview** grows a `$ / visit` column with an inline number input (admin can hand-edit any rate before saving; edited rates persist with `source='manual_override'` which the RPC then preserves against future auto-derivations) and a per-clinician source pill (`memory` / `derived` / `manual` / `⚠ unknown`). Med B amount = `Strata Vis × Rate`, recomputed live on rate edit. Save path persists `merged.per_clinician_rates` to the memory table via `upsert_clinician_medb_rates` (fire-and-forget, runs AFTER the audit save succeeds). **Phase 1 supersession:** the PR #84 dropzone now accepts `.pdf` only — `.xlsx`/`.csv` paths removed since the real Strata output is PDF. Migration 34 schema unchanged; the only behavior change is HOW `medb_total` is computed (was: direct stub column read; now: `Strata Vis × per-visit Rate`).
- ✅ **Clinician Payroll Audit — Strata Med B as parallel pay stream** (`finance.html` + `supabase/policies/34_clinician_payroll_medb.sql`) — adds **Med B** alongside regular Pay in the existing Clinician Payroll Audit so the portal reflects the full QB payable per clinician. Background: each pay period every clinician's payable is normally split across **TWO QB Bills sharing the same pay-period Bill #** (e.g. `05032026-05162026`) — one for regular Pay (TB) and one for Med B (Strata EMR), both routed to the same `Subcontractor:<discipline>` GL. **Import modal** gets a second dropzone (② Strata Med B — optional) underneath the existing ① TB Prepare-payroll dropzone; live state pill below it shows "No Strata file yet — Med B = $0" or "Loaded N clinicians · total $X" once parsed. Strata file is **optional** — saving without it produces `medb_total = 0` for every clinician (pre-migration behavior preserved). **Merge** is by normalized clinician name (existing `cprClinLookupKey` — strips trailing PT/PTA/OT/etc. suffix) so TB's "Adam Ouyang PT" lines up with Strata's likely "Adam Ouyang"; unmatched Strata names surface in a yellow warning strip in the preview ("⚠ N unmatched name(s)"). **Audit table** gains a new Pay / Med B / Total column triple (Pay = TB only, Med B = green-tinted Strata pay, Total = combined — what hits QB across the two Bills); per-delivery section headers now show `Pay $X + Med B $Y` totals; the header summary card grows a `+ MED B` and `PAY + MED B` row when Med B > 0. **History card** rows annotate the Pay cell with `Pay $X + Med B $Y` on a sub-line + show the combined total when Med B > 0; pre-migration rows display just Pay unchanged. **Schema (migration 34):** new `total_medb NUMERIC(12,2) NOT NULL DEFAULT 0` column on `clinician_payroll_runs`; per-clinician Med B lives **inside** the existing `per_clinician_payload` JSONB (new `medb_total` + `medb_source` fields, optional with 0/null defaults on read) — additive + forward-compatible. RPCs `upsert_clinician_payroll_run(...)` (adds `p_total_medb` mid-signature) + `list_clinician_payroll_runs()` (adds `total_medb` to return shape) both DROP-then-CREATE because of signature changes. Existing audits load with Med B = 0 across the board — no backfill needed. **Phase 1 scope (this PR):** import + display only. **Phase 3 (deferred):** QB Bill export will read `discipline` + `pay_total` + `medb_total` and emit two Bills per clinician sharing the period Bill # → both routed to `Subcontractor:<discipline>`. **Open item:** Strata Med B file column mapping is a STUB — today's parser assumes (column A = clinician name, column B = Med B $ amount) with auto-detected header row. Replace the COLUMN MAPPING block in `handleCprMedbFile` once the user shares a real Strata export.
- ✅ **🔧 QB Tools — new Finance sub-view with Bank Match tool** (`finance.html` + `supabase/policies/31_patient_check_deposits.sql` + `supabase/policies/32_payer_routing_memory.sql` + reuses existing `tb-scan-image` Edge Function) — net-new QB Tools sub-view under Finance, plus the **① Bank Match** card that handles four distinct payment-import workflows into a single SaaSant Bank Deposit XLSX export. **Inputs:** (1) **Chase bank CSV** — drop a Chase Online export, auto-classifies rows by carrier pattern (BCBS / Medicare / other) via regex on the bank description, extracts TRACE# + IND ID for audit refs into the Description/Memo; (2) **📷 Scan a patient check** — OCR-powered scan modal calls the existing `tb-scan-image` Supabase Edge Function (reused with a check-specific prompt) to extract payer/amount/check#/date/memo from a photo of a paper check, user confirms in a form before adding to the batch with a smart-default GL (AR for patients, Service Income for ~30 known carriers via `CARRIER_KEYWORDS` + a `remember_payer_routing` learn-as-you-go memory backed by migration 32); (3) **💳 Authorize.net CSV** — drop a Transaction Details by Settlement Date CSV (Expanded Fields/Comma Separated), tool groups rows by Settlement Date and emits one Bank Deposit per batch with a shared `Deposit No` (AUTH-YYYYMMDD); (4) collapsible **▾ Reconcile to bank** panel per Authorize.net batch with editable Funded Date + Fee (or linked Net) — Fee optional; if filled, a negative `Bank Service Charges:Credit Card Processing Fee` line gets appended per batch so each deposit nets to the actual Chase CYBERSR credit; "Clear all fees" button wipes all fee values without touching funded dates; localStorage persists per-batch reconcile values across reloads; second "⬇ Download bank-reconciled XLSX" button emits the funded-date version alongside the original v1 gross/settlement-date XLSX (both available so user keeps the raw transactions for reference). **Output XLSX:** 9-column SaaSant Bank Deposit schema (Deposit No / Date / Deposit To Account / Received From / Line Account / Line Description / Line Payment Method / Line Ref No / Line Amount) — line-level columns deliberately carry the `Line ` prefix per SaaSant's documented schema (article 14000053141) to avoid "Required parameter Line is missing in the request" errors. **③ Patient Deposit History** — separate card backed by migration 31 (`patient_check_deposits` table + 5 admin-only RPCs: insert/list/mark_cleared/update_notes/delete) that auto-logs every AR-routed scanned check (carrier checks + Authorize.net cc rows excluded) so the user can track which Strata EMR balances still need to be cleared manually. Sticky-thead table with payer/amount/check#/date/scanned-by/memo (inline-editable)/Strata-cleared toggle/delete; filter chips (Pending / All / Cleared); search by payer / memo / check#; pending count + sum-pending stats; one-click ✓ Cleared stamp with timestamp + actor; click-the-stamp-to-undo; two-click 🗑 delete. **Lessons baked in:** (a) per-transaction-type SaaSant dictionary — line-level fields need `Line ` prefix, the QB field key for Reference column is `Invoice No` not `Reference No/Name`; (b) single-word carrier name handling — `smartDefaultGl` strips spaces from both keyword and payer name before substring match (UNITEDHEALTHCARE matches "UNITED HEALTHCARE"); (c) scanner modal close-on-backdrop is mousedown+click double-check (so clicking inside an input doesn't accidentally dismiss); (d) per-row CC-customer nudge in the scan modal switches between amber "must exist in QB" (AR routing) and blue "no customer required" (Income routing); (e) reconcile panel uses surgical DOM updates rather than full re-render to avoid focus loss while typing in Fee/Net inputs; (f) `Authorize.net` should be created as a QB Vendor or Customer one-time so fee lines have a valid "Received From" target. **Map untouched.** Single SaaSant upload workflow handles everything — accountant drops Chase CSV + scans paper checks + drops Authorize.net CSV in the same session, then downloads one XLSX that creates the right mix of single-line and multi-line Bank Deposits in QB via SaaSant Bulk Upload → Bank Deposit.
- ✅ **🏢 Agencies — new admin-only portal tool** (`agencies.html` + `supabase/policies/30_agency_contracts.sql` + `index.html` shell wiring + Agencies link added to every tool's standalone nav strip) — replaces the user's local `HH RATINGS.xlsx` with a portal-native single source of truth for HH agency contracts. **Schema decisions:** sister table `home_health_agency_contracts` (not additive columns on `home_health_agencies` — keeps the map's table clean + leaves the door open for contract-history via `effective_from`/`effective_to` later); UNIQUE on `agency_id` for v1; admin-only RLS matching Finance posture. **UI:** sticky thead + sticky Name column; inline editors throughout (6 rate cells matching XLSX codes OTF/OTFA/PTF/PTFA/STE/STEO → DB columns `rate_ot_eval`/`rate_ot_assistant`/`rate_pt_eval`/`rate_pt_assistant`/`rate_st_eval`/`rate_st_other`; 2 click-to-cycle 1–5 rating chips with green/amber/red tint; Active toggle pill; 4 text inputs for contract location / pay method / sent via / collections contact; click-to-open notes drawer); search + Active/Inactive/All chips + Has/Missing-contract chips; stats strip shows total / has-contract / per-rating histograms. **📥 Import XLSX** modal accepts the user's existing `HH RATINGS.xlsx` shape unchanged — SheetJS parses sheet `Agencys`, client-side preview shows match-vs-unmatched counts using a normalized-name comparison, unmatched rows get a typeahead-style dropdown to manually pick from existing portal agencies or skip, then `upsert_agency_contracts` RPC does the authoritative server-side match via the new `normalize_agency_name()` IMMUTABLE helper. **Inline saves** use a new `update_agency_contract(p_agency_id, p_field, p_value JSONB)` RPC that whitelists fields and type-coerces JSONB scalars into NUMERIC/SMALLINT/BOOLEAN/TEXT via dynamic SQL with type-safe USING binding; auto-creates the contract row on first edit so UI doesn't need separate insert/update code paths. Agency name link uses `parent.shellSwitchTab('map', 'agency=...')` to open the existing map agency profile in shell, else opens in a new tab. **Map untouched** — no rate/payment chips on map profile in this rev (user explicitly said "lets not add fields to the map yet"). Two-layer admin gate matches Finance: shell tab hidden for non-admin + page-level role check shows a "🔒 Admin only" gate card. New reusable `normalize_agency_name()` helper (lowercases, drops punctuation, strips Inc/LLC/Ltd/Corp/Services/Co/PC suffixes, collapses whitespace) lives server-side so future matchers can share it. Future hooks captured in code comments: (a) drop unique-on-agency-id + add `effective_from`/`effective_to` for contract history; (b) surface rate chips on the map's agency profile; (c) bill-side auto-population from the per-discipline rate columns.
- ✅ **Finance → Billing — ⑤ Attachments Bundler (SaaSant zip)** (`finance.html`) — new card downstream of ④ Invoice Converter that bundles TB invoice PDFs with a SaaSant-compliant Excel index so accounting can attach the source invoice to each QuickBooks invoice after import. Two dropzones: ① the IIF (raw or cleaned — parser auto-detects DOCNUM or "Invoice No" header) → extracts unique invoice numbers; ② multi-select PDFs (drop a whole folder). Filename matcher is smart-strip aware: tries exact stem match first, then strips common `INV-` / `INVOICE-` prefixes (case-insensitive) and retries. Live stat strip shows invoices / PDFs / matched / unmatched-invoice / unmatched-PDF counts; preview table lists matches + flags gaps. **Download SaaSant zip** uses ExcelJS for the index (`Reference Number` + `File Name` columns, header bold) and JSZip (added to the CDN list) to bundle index + matched PDFs into `{base}_SaaSant_Attachments.zip`. Optional coverage — gaps warn but don't block download (per user spec). Admin-only via the existing `currentRole==='admin'` gate on `ATTACH.init()`. Implements the [SaaSant attachment upload workflow](https://support.saasant.com/support/solutions/articles/14000141689) (separate step after the IIF import lands the invoices in QB).
- ✅ **Eval Compliance — supervisor-only Clinician cell** (`compliance.html` → `renderClinNames`) — since evals are a supervisor's job (PT / OT / ST / SLP / etc.), the Clinician column now hides assistants by default. The cell renders only supervisor name(s); when assistants are also assigned, a small `👥 +N assistant(s)` dashed chip below the supervisor shows their names in a `title` tooltip on hover. Classification is by the trailing credential code per `;`-separated name: assistants = `PTA OTA STA COTA SLPA LPN CNA HHA`, everything else is treated as a supervisor. Fallback: rows where TB only listed an assistant (no supervisor identifiable) still show all names so nothing disappears silently.
- ✅ **Eval Compliance — auto-promote category to 'Completed' on sync (migration 29)** — `apply_eval_visits` now flips `category` to `'Completed'` in lockstep with `sync_status='synced'`, so rows don't keep sitting in Issues / To Sync / No Sched / etc. after their eval is done and synced. Hold/Hospital is preserved (patient off-service; the sync may be a prior eval still on file). One-shot backfill clears the existing backlog of stale "needs attention" entries in one go.
- ✅ **Eval Compliance — episode-aware visit matching (migration 28 bug fix)** — For returning patients with multiple referrals, `apply_eval_visits` was matching on `(patient_id, service)` only, so an eval visit from a prior episode would attach to the current row (the user spotted a row where the referral date was 6/1/2026 but the eval was logged as 4/8/2026 — confirmed via the Patient Visits CSV's `Start of episode` column showing 3/20/2026 for that visit). Migration 28 replaces the RPC with episode-aware matching `(patient_id, service, episode_start_date)` + a one-shot cleanup that wipes provably-mismatched eval data (where the attached eval date predates the row's own episode start). `compliance.html` adds `epstart` to the required-columns list for Patient Visits CSV and threads `episode_start_date` through the aggregator bucket key and the RPC payload.
- ✅ **Eval Compliance — Proposed Eval column + drift tracking** (migration 27 + `compliance.html`) — employees can now enter a **planned eval date** in the portal before creating the appointment in TB. The new column lives right of the existing "Eval Sched" and renders one of four states: Empty (`+ Propose` button), Awaiting (⏳ proposed but TB hasn't picked it up yet), Confirmed (~~strikethrough~~ + ✓ when proposed matches TB's `next_eval_date`), or Drift (`~~Proposed 5/22~~ → TB: 5/26 ⚠` when TB shows something different). Inline editor on `+ Propose` / `✎` lets staff enter date + optional clinician + optional note. `✓ Ack` button stamps the row's `last_acknowledged_at/by` and writes an audit row. Match rule = date only (clinician mismatch doesn't break confirmation). All transitions logged to a separate `evaluation_proposed_history` audit table via SECURITY DEFINER RPCs; client-side `📜` opens a history popover. Confirmation/drift-ack history is written only on explicit click — never auto-emitted by imports, so the audit trail stays signal-only.
- ✅ **Clinician LLC + EIN** (migration 26 + Roster Review + Clinician Map + Finance) — some clinicians operate through their own LLC; the QB Bill needs to be made out to the LLC name (not the person) and accounting needs the EIN for 1099s. Two new nullable columns on `clinician_profiles`: `llc_name TEXT` + `llc_ein TEXT`. Roster Review has two new columns ("LLC Name" default-on, "EIN" default-off opt-in via Columns ▾) — both inline `<input>` cells with their own optimistic-update save (`saveLlcName` / `saveLlcEin`). The LLC name surfaces as a 🏢 sub-line on the **Map profile header**, the **Clinician Payroll Audit** per-clinician rows, and the **Payroll Overview reference card**. EIN displays only in Roster Review. `mergeClinicianProfileRecord` in the map carries the new fields onto the in-memory clinician object.
- ✅ **Clinician payment classification — two orthogonal axes** (migration 25 + `roster-review.html` + `finance.html`) — payment is no longer one of (check/dd/zelle/w2). Now it's two independent fields: `payment_method` ('bill' = QB Bill | 'w2' = W-2 Payroll) for HOW QuickBooks records it, and `payment_delivery` ('paper_check' | 'direct_deposit' | 'zelle') for HOW the money moves. So a clinician can be (W-2 + DD) or (QB Bill + DD) or (QB Bill + Paper Check) etc. Migration 25 also runs a one-shot data translation of legacy values (check → bill+paper_check, dd → bill+dd, zelle → bill+zelle, w2 → w2+dd best-guess). Roster Review row gets two side-by-side dropdowns (Bill Type | Delivery), each with its own optimistic-update save (`savePayBillType` / `savePayDelivery`). Finance audit + Overview card refactored: collapsible sections + summary group BY DELIVERY (3 buckets — 📭 Paper Check / 🏦 DD / 💸 Zelle), and each clinician row carries TWO chips (📄 Bill or 🧾 W-2 + the delivery chip). A second sub-strip in the audit summary shows the bill-type breakdown ("📄 27 Bill · 🧾 3 W-2"). Constants renamed: `CPR_BILL_TYPES` + `CPR_DELIVERIES`, `cprPaymentByName` Map now returns `{bill, delivery}`.
- ✅ **Payroll area — 👥 Clinicians by Pay Method reference card** (`finance.html` → Finance → Payroll) — adds a collapsible card at the top of the Payroll sub-view that lists every Active clinician grouped by `payment_method`. Card starts collapsed; meta line shows total + per-method counts ("30 active · 📄 23 · 🏦 4 · 💸 2 · 🧾 1"). Click the card to expand, then click any per-method section header (📄 Check / 🏦 DD / 💸 Zelle / 🧾 W-2 / ❓ Unmapped) to drill into the names + discipline pills. Each clinician row has a small ↗ Roster link that opens Roster Review for quick edits. Single round-trip on first expand (joined `clinician_v2` + `clinician_profiles!inner`), cached in `cpoClinicians`; 🔄 button re-fetches after roster edits. Reuses the `CPR_PAY_METHODS` palette.
- ✅ **Clinician Payroll Audit — collapsible quick-view sections by pay type** (`finance.html` → `cprRenderDetail`) — the per-clinician table is now grouped into tinted collapsible section headers, one per payment method (📄 Check / 🏦 DD / 💸 Zelle / 🧾 W-2 / ❓ Unmapped), **all closed by default**. Each header shows count + dollar total ("🏦 Direct Deposit · 4 clinicians · $X,XXX"); click to expand. Multiple sections can be open independently. Search auto-expands any bucket with matches (manually opened set stays put when search clears). State lives in `cprViewState.expandedMethods` so it survives all the in-detail edits (visit exclude, manual adjustments, per-visit pay edits, etc.) via the existing render path.
- ✅ **Clinician Payroll Audit — payment-method chips + per-method summary** (`finance.html` → Finance → Payroll → 💵 Clinician Payroll Audit) — when opening an audit, fetches a name → `clinician_profiles.payment_method` lookup once (`cprLoadPaymentMethods`), then renders a color-tinted chip after each clinician name in the per-clinician table (📄 Check / 🏦 DD / 💸 Zelle / 🧾 W-2) and a summary strip above the table showing per-method counts + dollar totals (incl. an `❓ N Unmapped` chip that flags any TB-export-name vs `clinician_v2.name` drift before Phase 3 export). Name normalization strips trailing discipline-code suffixes (PT/PTA/OT/etc.) so TB names like "Basma Rafael PT" map to "Basma Rafael" in the roster. Also flipped the **Pay Method** column in Roster Review to **default-on** (was opt-in via the Columns ▾ picker) so the field is visible day-to-day. No SQL — migration 20 already provides the data.
- ✅ **Office-staff birthdays + scheduled vacations on News & Alerts** (`index.html` Home + Time Tracker Settings) — admin enters office employees' birthdays via a new 🎂 **Birthday** field on each staff row in the Time Tracker Settings tab (`staff_config.dob`). The Home page 📰 News & Alerts board now also surfaces office-staff birthdays (today/tomorrow) and scheduled vacations (groups consecutive `absence_kind = 'vacation'` days from `time_entries` into vacation blocks; shows blocks happening now or starting in the next 14 days). Migration 24 adds the `dob` column + `list_staff_birthday_alerts` / `list_staff_vacation_alerts` RPCs. Renderer distinguishes staff alerts (`staff_*` alert types) from clinician alerts and routes click-through to the Time tab instead of the Map. Also renamed the **Day Off** column → **Time Off** in the My Timesheet card.
- ✅ **Time Tracker — My Timesheet auto-populates from clock-ins + override semantics** (`time-tracker.html`) — staff no longer re-enter hours that were already clocked. The My Timesheet rows now pre-fill the Hours cell with each day's clock-in/out sum (computed live from `time_entries` where `is_manual = false`). A tiny "Auto: X.XXh" hint sits below each input — it strikes through when the staff types a different value, signaling an active override. `tsSubmit` only persists a `Timesheet|` override when the typed value differs from auto (or there's an absence/note); matching auto = no row inserted, auto value stands. `hoursInPeriod` rewritten with **per-day override semantics**: for any day with a `Timesheet|` entry the override sum replaces the clock-in sum (no double-count). Admin Shifts modal splits the type badge into **System** (clock-in shift, grey) / **Timesheet** (staff override, blue) / **Manual** (admin aggregate, gold) so the admin can tell at a glance which row drove the payroll number — original clock-ins remain visible alongside the override. Modal's "total hours" line now uses `hoursInPeriod` so it matches payroll.
- ✅ **Time Tracker — day-off / absence tracking** (`time-tracker.html` → My Timesheet) — staff can now mark missed days with a reason via a per-row **Day Off** pulldown: 🏖 Vacation / 🤒 Sick / 👤 Personal / ⚡ Power Outage / 📶 Internet. Migration 23 adds `absence_kind TEXT` to `time_entries` with a CHECK constraint over the 5 allowed values. Absence-only days store as a zero-duration row (clock_in == clock_out + absence_kind set) so payroll totals are unaffected; partial-day absences (hours > 0 + reason) also work. Row gets an amber tint when a reason is set; admin Shifts modal shows an amber chip next to the Type cell. No additional UI gating — the dropdown is the source of truth for allowed values.
- ✅ **Time Tracker — pay-period approval workflow** (`time-tracker.html` → staff My Timesheet + admin Dashboard / Pay Period / Shifts modal) — adds an explicit "this period is done" signal between staff and admin. Migration 22 (`pay_period_submissions` table with 4-state machine: `draft` → `submitted` → `approved`/`rejected` + 4 SECURITY DEFINER RPCs). Staff: status pill above the timesheet (Draft / 🟢 Submitted / ✓ Approved / ⚠ Sent back with note); Submit-button label flips to Re-submit / Submit Fixes; `tsSubmit` calls `submit_pay_period` after the per-day inserts so per-day rows + per-period status flip together. Admin Dashboard cards: pill next to IN/OUT + inline **✓ Approve Period** button when submitted. Admin Pay Period rows: same pill next to each staff name + inline ✓ Approve. **Shifts modal upgrades**: bug fix — was using the Pay Period tab's `periodOffset` so opening from Dashboard after viewing a past period showed wrong shifts; now always `getPayPeriod(0)`. OPEN shifts show **live running hours** via a 1s `shiftsLiveTimer` updating `data-running-since` cells in place; period total counts open shifts at "now". New **Adjustments & Receipts** section below the shifts table (Date / Kind chip / signed amount / note with ₱→USD reference / 📎 receipt link). Approval footer with pill + meta + ✓ Approve / ↩ Send Back (`window.prompt` for the rejection note). Approval is a stamp, not a hard lock — admin can still edit after approval. Caveat carried forward: admin inline-hours edit still wipes daily Timesheet rows; tag-aware version is a follow-up.
- ✅ **Time Tracker — "My Timesheet" for staff self-entry** (`time-tracker.html` → staff view) — replaces the emailed Excel timesheet (Day / Date / Hours / Notes × 10 working days) with a portal-native card in `#staff-view`. 14-day grid for the current pay period (Mon-Fri normal, Sat/Sun visibly muted), per-day Hours + Notes inputs, live period total in a gold-tinted footer row, ◀/▶ period nav, and a **Submit Timesheet** button. Stores per-day `time_entries` with `is_manual=true` and `manual_note='Timesheet|{optional note}'` — the `Timesheet|` tag lets the staff side manage its own rows independently of the admin's aggregate (`Manual hours entry`). Direct submit (no pending state); employees can re-submit until the period closes. Admin Pay Period `hoursInPeriod` sums daily rows naturally into the existing HOURS cell. **Caveat**: admin inline edit still wipes ALL is_manual rows in the period (existing behavior) — if the admin types a total while daily timesheet rows exist, the daily rows are replaced by the aggregate; employee can re-submit. Tag-aware admin inline edit is a documented follow-up.
- ✅ **Time Tracker — adjustments as individual sendable rows + reimbursements** (`time-tracker.html` → Pay Period) — positive adjustments (bonuses/reimbursements) now render as their own **sendable sub-row beneath each person** (own grossed-up Send-via-Wise + 📋 Wise memo via `buildAdjMemo`), and the person's main row shows **base pay only** (hours × rate); negative deductions fold into the base. Each person row has an inline **+ adj** button (pre-fills the adjustment modal to that staff) alongside the top "+ Adjustment". New **Reimbursement** adjustment type with a 🇵🇭 PHP / 🇺🇸 USD currency toggle + live PHP→USD conversion (uses the Pay Period PHP rate; stores USD in `amount`, keeps the original peso amount + FX rate) and an optional **receipt upload** (📎 link on the row). Grand totals are unchanged by the row split. Migration 21 (`pay_period_adjustments` columns + `reimbursement-receipts` public bucket).
- ✅ **Time Tracker — Wise payment memo** (`time-tracker.html` → Pay Period) — each 🇵🇭 Wise row has a 📋 copy button next to "Send via Wise" that copies a special-char-free description for Wise's memo field, e.g. `Payroll 0427 to 0508 40 X 466` (period MMDD→MMDD · hours · X · rate). `buildWiseMemo()` formats dates as MMDD in ADMIN_TZ, drops the rate's decimal (`4.66`→`466`), and replaces a fractional-hours dot with a space (`40.5`→`40 5`); a `[^A-Za-z0-9 ]` strip guards against any Wise-illegal char. Click-to-copy uses `navigator.clipboard` with a hidden-textarea `execCommand` fallback; delegated on the tbody. 🇺🇸 "Direct payment" rows have no button.
- ✅ **Time Tracker — inline manual hours entry** (`time-tracker.html` → Pay Period) — removed the "✏ Enter Hours" popup + `#bulk-hours-modal`; the HOURS cell in the pay table is now a direct inline `<input>` per active staff row (commit on blur/Enter via `saveManualHoursForStaff()`, writing the same synthetic `is_manual` time_entry; 0 clears the manual entry, real clock-in/out shifts untouched). Added a sort/filter toolbar above the table: debounced **search by name**, **sort A–Z**, and **pay-type chips** (All / 🇵🇭 Wise / 🇺🇸 USD). Grand totals always compute over all active staff (search/filter-independent); the opposing total row hides when a pay-type filter is active. Pay Period tab is already admin-gated (`#admin-view`).
- ✅ **Clinician Payroll Audit + payment-method tagging** (`finance.html` → Payroll → 💵 Clinician Payroll Audit) — TB "Prepare payroll" Excel import with SUBMITTED-date period detection (we pay by sync date, not visit date) and a "most visits wins" target picker + yellow multi-period sanity warning. Per-clinician audit table with sticky thead, title-cased names, search-to-isolate, exclude/restore visits, free-form adjustments (date/desc/amount), per-visit pay editing in place with redline markup (strikethrough original + bold new + ↶ revert), auditor flags ($0 / missing $ / late-sync / short visit / revisions). Audit state machine open → audited → exported → paid with timestamps + actor IDs (Reopen returns to open); all edit affordances gate off status. Calendar drives the workflow — click a period → opens audit (View if a run exists, Import if not); "AUDIT NOW" pill on the period whose pay date is the next upcoming Monday. History row delete button (two-step confirm). Migration 19 (table + 7 admin-only RPCs) + Migration 20 (`payment_method` column with 4-value CHECK constraint, default 'check', plus Roster Review opt-in "Pay Method" column with inline dropdown for tagging clinicians as check / direct_deposit / zelle / w2 to drive Phase 3 export grouping).
- ✅ **Portal nav 🏠 Home link** on every tool's standalone header — first link in the nav, points to `index.html` (shell's Home dashboard pane). Hidden by `.in-iframe-shell` CSS when running inside the shell.
- ✅ **Map: Isolate Clinician button state fix** — `toggleClinicianIsolation()` now syncs both profile header isolate buttons after toggle (was only calling `applyFilters()`, leaving the button stuck on its inactive label/style despite the underlying filter actually working).
- ✅ **Roster Review built + polished** (`roster-review.html`) — sticky thead + sticky Name column for big-table scrolling, inline pause editor with Personal/Sick/Vacation/Other dropdown, EMR chips click-to-toggle (writes to `clinician_profiles.emr_capabilities` JSONB — same surface as the map), Phone/Email locked with 🔒 indicator (TB source-of-truth), Emergency Contact drawer (managed in portal since TB doesn't track it), discipline + status pill stacked under each name. Restrictions drawer uses roster-only typeahead — no free-text. Performance metrics (Avg Sync Days / Avg Visits/Run / Avg Visit Time) prioritized right after Status with green/amber/red tint on sync days. Surgical EMR toggle keeps scroll locked; renderTable() preserves scroll + focus across re-renders to prevent view-jump on edits.
- ✅ **Finance area** (`finance.html`, admin-only) — landing page with Payroll + Billing tiles. Payroll surfaces current pay period totals + link to Time Tracker. Billing now contains four cards: VDR Runner / Inline Preview / History & Trends / Invoice Converter (IIF).
- ✅ **Billing → VDR Runner** — browser-side port of the user's local `Processed_VDR.py`. SheetJS for input, ExcelJS for output, Chart.js for inline charts. Persists every saved run + per-clinician + per-payer aggregates. 📊 View button on history rows rebuilds the inline preview from a stored `prepared_rows` JSONB snapshot. Replaces the legacy Processed_VDR aggregator script (filename-scanner) with a single SQL query + trend chart.
- ✅ **VDR per-clinician metrics surfaced on Roster + Map profile** — `09_clinician_vdr_summary.sql` RPC rolls up `vdr_clinician_metrics` per clinician (visit-weighted avgs). Default-on columns in Roster Review; three chips (Sync / Visits/Run / Avg Visit Time) in the map's profile-header quick-facts row.
- ✅ **Patient Visits-derived metrics (Roster + Map)** — `17_clinician_visit_period_metrics.sql` adds a per-(clinician, pay_period) aggregation populated by the Eval Compliance Patient Visits import OR a separate admin-only **📥 Bulk Import Visits** button on Roster Review (one-shot multi-year backfill, handles ~270K rows). `list_clinician_visit_summary()` mirrors the VDR-summary shape so Roster + Map use the same cell renderers — Visits is preferred, VDR is fallback. Both pages strip trailing discipline codes (PT/PTA/OT/OTA/ST/STA/SLP/OTR/RN/LPN/CNA/MD/DO/DPT/NP/PA) on name-based lookup to match TB's "Basma Rafael PT" format against `clinician_v2.name`'s "Basma Rafael".
- ✅ **Billing → Invoice Converter (IIF)** — browser-side port of the user's local IIF cleanup Python. Drag-drop a raw .iif, strip 11 cruft columns, drop TAXABLE/negative rows, rename headers, Medicare → Medicare Part B. substitution, download cleaned `Dependable_Invoice_<YYYY-MM-DD>.iif` for upload to SaaSant → QB.
- ✅ **Shared cursor on map** — toolbar toggle + Shift-hold hotkey broadcast your cursor (lng/lat) via Supabase Realtime broadcast channel `map-cursors-v1`. Other map viewers see colored cursor + name label. Works across different zoom levels. 25Hz throttled, no DB writes.
- ✅ **Portal shell** — `index.html` is now a real iframe shell hosting 7 tools (Map / Referrals / Time / Compliance / Kanban / Roster / Finance) + a Home dashboard pane. State preserves across tabs. Finance tab is admin-only-revealed.
- ✅ **Self-service signup + forgot/reset password** — full 4-mode auth flow in the shell login screen.
- ✅ **Auto-creation triggers** — `05_auto_user_role.sql` and `06_auto_staff_config.sql` mean every signup gets a readonly role + inactive staff_config row.
- ✅ **Time Tracker — "Needs setup" section + editable display names + auto-refresh on Settings tab**. Pay Period view now has a diagnostic strip showing UTC bounds + query/in-period row counts to debug period-filter issues.
- ✅ **Chat presence debounce + iframe guard** — refresh/tab-switch no longer fires join/leave spam; widget only renders at shell level.
- ✅ **Manage Users modal in clinician-map** — switched to `list_staff()` RPC + upsert for role changes.

### Still pending
- **Phase 1 RLS** — 23 tables locked. Three tables still UNRESTRICTED (`clinician_referrals`, `clinician_territory_versions`, `pay_period_adjustments`) — easy follow-up SQL.
- **Phase 2 RLS** — per-role policies (admin/editor/readonly) tied to `user_roles`.
- **Storage bucket RLS** — `clinician-photos` bucket needs its own policy review.
- **`tasks-auto-archive` Edge Function** — written, not yet deployed.
- **Time Tracker Pay Period bug** — user reported "whatever period I select displays the same data" with hours that belong to a prior period showing in newer period view. Diagnostic strip shipped to investigate; needs reproduction data to root-cause.
- **Shell-aware clinician open from Roster** — clicking a clinician name in Roster currently spawns a new browser tab. Plan captured in `restrictions-as-in-agencies-shiny-kahan.md` "Future work" — switch to `parent.shellSwitchTab('map', 'clinician=…')` when in shell.
- **VDR — Phase 2 direct QB connect** — Plan in `its-used-to-import-merry-rabbit.md`. Bypasses SaaSant via QuickBooks Online API + Supabase Edge Function. ~1-2 weeks of work. Decision: pending SaaSant pain experience.
- **Roster — manual unmatched clinician linker** — VDR's auto-name-match leaves `clinician_id = null` if a TB export name doesn't match `clinician_v2.name` exactly. Need a small admin UI to manually link names. Currently aggregates still flow via the name-normalized fallback so this isn't urgent.
- **time-tracker.html** — Task #4: time edit approval system + 10hr hard stop (partially built, needs testing/polish).
- **compliance.html** — not yet explored or documented.
- **Kanban v2 ideas** — auto-tasks, comments, file attachments, @mentions, recurring tasks.
- **Chat v2 ideas** — multiple channels, DMs, file/image uploads, @mentions, read receipts, edit/delete UI.
